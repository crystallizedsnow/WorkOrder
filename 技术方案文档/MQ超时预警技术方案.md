# 工单超时提醒简化技术方案

## 1. 方案结论

结合当前项目规模、工单超时提醒的实时性要求和已有 RabbitMQ、站内消息、飞书投递能力，本期不新增定时事件表，不引入 XXL-JOB，也不使用 RabbitMQ 延迟消息插件。

本期采用：

```text
@Scheduled 每30秒扫描工单表
  -> 按索引分批查询已超时工单
  -> 条件更新状态为已超时（410）
  -> 生成站内消息和飞书投递任务
  -> RabbitMQ异步处理通知
```

正常情况下，工单超时识别延迟为 0～30 秒。考虑任务执行、RabbitMQ消费和飞书发送耗时，目标是在系统正常时 1 分钟内完成用户提醒。

该方案是在现有实现上的增量优化，重点解决扫描间隔过长、单次处理无上限、任务可能重叠以及通知可靠性不足的问题。

## 2. 背景与现状

### 2.1 当前实现

`WorkOrderServiceImpl.checkAndUpdateOverdueOrders()` 当前每 5 分钟执行一次：

1. 查询截止时间已到的工单；
2. 只处理“处理中（400）”和“验收失败（670）”；
3. 条件更新工单状态为“已超时（410）”；
4. 查询尚未完成的处理人；
5. 通过 RabbitMQ 生成站内消息；
6. 通过 `notification_delivery` 生成并重试飞书通知。

现有超时状态条件更新已经能够避免同一工单被多个实例重复转换，现有飞书投递表也具备一定的失败重试和接收人幂等能力，因此没有必要重建完整调度体系。

### 2.2 当前主要问题

1. 5分钟扫描产生0～5分钟的识别延迟；
2. 一次查询所有到期工单，缺少批量上限；
3. 工单表没有针对状态和截止时间的联合索引；
4. `@Async`与`@Scheduled`组合可能造成任务重叠；
5. 整批工单共用长事务时，一条异常可能影响其他工单；
6. RabbitMQ发布确认、消费幂等和失败告警不完整；
7. 飞书投递处于`SENDING`时服务退出，任务可能无法自动恢复。

## 3. 目标与非目标

### 3.1 建设目标

- 将超时识别延迟从0～5分钟降低到0～30秒；
- 系统正常时，站内消息和飞书提醒在超时后1分钟内发起；
- 每轮扫描和处理数量有明确上限；
- 支持后端多实例运行，不重复修改状态和创建通知；
- RabbitMQ或飞书短时异常后，通知能够重试或被发现；
- 单条工单处理失败不影响整批工单；
- 尽量复用现有表、代码和中间件，控制改造成本。

### 3.2 非目标

本期不包含：

- 临近截止提醒；
- 新增定时事件表；
- 秒级精确触发；
- XXL-JOB、Quartz等调度平台；
- RabbitMQ延迟交换机插件；
- 多级SLA、节假日日历和自动升级负责人；
- 将业务流程状态和SLA状态拆成两个独立状态体系。

## 4. 业务规则

### 4.1 超时条件

工单同时满足以下条件时视为超时：

- `deleted = 0`；
- `deadline_time IS NOT NULL`；
- `deadline_time <= 当前时间`；
- `status IN (400, 670)`。

超时后将状态更新为410。只有条件更新影响一行的实例，才有资格生成通知。

### 4.2 接收人

提醒对象为所有：

- `handle_type = HANDLE(4)`；
- `finished = false`；
- `deleted = 0`；
- `user_id`非空的处理人。

接收人按`user_id`去重。已经完成个人处理任务的协助人不再提醒。

### 4.3 截止时间修改

本方案不提前创建未来事件，因此截止时间修改不需要取消旧消息。下一轮扫描直接以数据库最新的`deadline_time`为准。

如果工单被延期到未来时间，不会被扫描命中；如果截止时间被提前到当前时间之前，下一轮扫描会在30秒内处理。

### 4.4 完成和取消

扫描和条件更新均校验当前状态。工单在扫描前已经完成、验收、取消或删除时，不会被更新为410，也不会产生超时提醒。

扫描查询与实际更新之间如果发生并发状态流转，最终由条件更新保证只有仍处于400或670的工单才能转为410。

## 5. 总体流程

```text
Overdue Scanner（每30秒）
  -> 按(status, deadline_time, id)索引查询
  -> 按id升序，每批最多200条

Overdue Handler（逐工单、小事务）
  -> 条件更新400/670 -> 410
  -> 更新失败：说明已被其他实例或业务操作处理，跳过
  -> 查询未完成处理人并去重
  -> 创建站内消息和飞书投递记录

Notification Dispatcher
  -> 站内消息通过RabbitMQ异步落库
  -> 飞书投递调用AiAssistant
  -> 记录成功、重试、失败或跳过

Recovery Scanner（每5分钟）
  -> 恢复卡住的通知任务
  -> 发现异常积压并告警
```

## 6. 定时扫描设计

### 6.1 调度方式

继续使用Spring `@Scheduled`，默认配置：

```text
fixedDelay = 30秒
```

选择`fixedDelay`的原因是下一轮会在上一轮结束后再等待30秒，天然降低同一实例内任务重叠概率。

扫描方法不再使用`@Async`。扫描只负责分批查询和调用处理器，真正的消息通知由RabbitMQ和投递服务异步完成。

### 6.2 批量查询

每次查询最多200条，按`deadline_time, id`排序。推荐查询条件：

```text
status IN (400, 670)
AND deadline_time <= now
AND deleted = 0
ORDER BY deadline_time, id
LIMIT 200
```

处理完一批后可以继续处理下一批，但单轮最多处理10批，即最多2000条，防止扫描线程长期运行。仍有积压时由下一轮继续处理。

### 6.3 数据库索引

在`work_order`增加联合索引：

```text
idx_work_order_overdue(status, deadline_time, id)
```

索引使数据库能够直接定位特定状态下已经到期的记录，避免每30秒扫描整个工单表。

上线前使用实际数据执行`EXPLAIN`，确认查询命中联合索引且没有全表扫描。

### 6.4 多实例并发

不额外引入分布式锁。多个实例允许同时查到同一工单，但更新时使用条件更新：

```text
UPDATE work_order
SET status = 410
WHERE id = ?
  AND status IN (400, 670)
  AND deadline_time <= now
  AND deleted = 0
```

只有一个实例能够成功更新。其他实例受影响行数为0，直接跳过，不生成通知。

这种方式不能完全避免重复查询，但在当前TPS和工单规模下，重复查询成本低于引入分布式调度或锁服务的复杂度。

## 7. 事务设计

### 7.1 事务边界

每张工单单独使用短事务处理：

1. 条件更新状态为410；
2. 查询当前未完成处理人；
3. 为接收人创建通知投递记录；
4. 提交事务。

RabbitMQ网络调用和飞书HTTP调用不放在该数据库事务内。

这样一张工单失败只回滚自身，不影响同批其他工单，也不会因为等待外部系统导致数据库事务长时间占用连接和锁。

事务方法应放在独立Spring Bean中，由扫描器调用，确保`@Transactional`代理生效，避免同类内部调用导致事务注解失效。

### 7.2 通知投递记录

复用现有`notification_delivery`表，不新增定时事件表。每次工单首次成功转为410时，为每个接收人创建投递记录。

建议支持两个渠道：

- `MESSAGE_CENTER`：站内消息；
- `FEISHU`：飞书卡片。

唯一约束继续使用：

```text
(event_type, work_order_id, receiver_id, channel)
```

其中`event_type`固定为`WORK_ORDER_OVERDUE`。由于当前业务约束是同一工单只首次进入410时提醒一次，该唯一键足以防止重复投递。

推荐状态：

```text
PENDING -> SENDING -> SUCCEEDED
                    -> RETRYING
                    -> FAILED
                    -> SKIPPED_UNBOUND
```

投递记录与工单状态更新在同一事务提交，因此即使服务在提交后立即退出，待发送任务仍然保存在数据库中。

## 8. RabbitMQ与站内消息

### 8.1 使用方式

RabbitMQ不承担30秒计时，只负责超时确认后的异步通知：

```text
notification_delivery(MESSAGE_CENTER, PENDING)
  -> 通知发布器发送普通持久化消息
  -> workOrder.message.queue
  -> WorkOrderMessageConsumer
  -> message表
```

### 8.2 发布可靠性

RabbitTemplate增加：

- Publisher Confirm；
- Publisher Returns；
- mandatory路由检查；
- 持久化消息；
- 发布失败日志和指标。

收到broker confirm后，将对应投递记录标记为`SUCCEEDED`。发布失败或confirm超时则标记为`RETRYING`，由通知发布器稍后重试。

如果出现“MQ已经收到，但数据库状态回写失败”，允许再次发送同一通知消息，因此消费者还需要幂等处理。

### 8.3 消费幂等

MQ消息增加稳定的通知投递ID或业务幂等键。`message`表保存该标识并建立唯一约束，重复消费时直接返回成功，不重复插入站内消息。

消费者成功落库后ACK；短时数据库错误使用有限重试；无法解析的消息进入死信队列，禁止无限`requeue`。

## 9. 飞书通知

继续复用现有`notification_delivery`和AiAssistant飞书发送能力。

超时卡片展示：

- 工单编号；
- 工单标题；
- 原截止时间；
- 当前状态：已超时；
- 请尽快完成的提示。

投递规则：

- 只向有效飞书绑定发送；
- 未绑定用户标记`SKIPPED_UNBOUND`，但保留站内消息；
- 网络错误、限流和5xx有限重试；
- openId无效、权限不足等永久错误直接失败；
- 同一投递记录只允许一个实例从`PENDING/RETRYING`抢占为`SENDING`；
- `SENDING`超过2分钟自动恢复为`RETRYING`；
- 达到最大尝试次数后标记`FAILED`并告警。

为了满足1分钟内发起通知的目标，飞书待发送任务扫描周期从30秒调整为5～10秒。该查询使用现有`(channel, status, next_retry_time)`索引并限制批量数量，开销可控。

## 10. 配置建议

```yaml
workorder:
  overdue:
    scan-delay-ms: 30000
    batch-size: 200
    max-batches-per-run: 10
  notification:
    dispatch-delay-ms: 5000
    batch-size: 100
    max-attempts: 3
    initial-retry-delay: 1m
    sending-lease: 2m
```

扫描周期配置化，但生产环境应限制最小值，避免误配置成几十毫秒造成数据库压力。

建议先使用30秒，根据实际查询耗时、数据库负载和业务反馈决定是否调整为15秒或60秒。

## 11. 可观测性

### 11.1 指标

至少记录：

- 每轮扫描耗时；
- 每轮查询和成功更新工单数量；
- 条件更新竞争失败数量；
- 最老未处理超时工单的延迟；
- 各渠道`PENDING/SENDING/RETRYING/FAILED`数量；
- RabbitMQ发布成功率、失败率和confirm耗时；
- MQ队列深度、消费速率和死信数量；
- 飞书成功率、限流率和发送耗时。

### 11.2 告警

建议在以下情况告警：

- 扫描任务连续多次失败；
- 最老未处理超时工单延迟超过2分钟；
- 通知待发送数量持续增长；
- `SENDING`超期记录持续出现；
- RabbitMQ无消费者或发布连续失败；
- 死信队列出现消息；
- 飞书失败率明显高于历史基线。

日志包含`workOrderId`、`workOrderCode`、`receiverId`、`channel`和投递记录ID，不记录Token、密钥和完整飞书身份信息。

## 12. 测试方案

### 12.1 单元测试

- 只查询状态400/670且已超过截止时间的工单；
- 扫描查询最多返回配置的批量数量；
- 条件更新失败时不创建通知；
- 只查询未完成处理人并按用户去重；
- 同一工单、接收人和渠道重复创建投递记录时命中唯一约束；
- 单条工单异常不影响其他工单；
- `SENDING`超时任务能够恢复；
- MQ重复消息不会重复写站内消息。

### 12.2 集成测试

- 工单到期后30秒内转换为410；
- 两个后端实例同时扫描只生成一组通知；
- 扫描查询与业务完成并发时不误改状态；
- 一次到期超过200条时能够分批处理；
- RabbitMQ短时不可用后站内消息能够重试；
- 消费者处理成功但ACK前退出时不会重复入库；
- 飞书失败不回滚工单状态和站内消息；
- 未绑定用户只有站内消息；
- 服务在飞书任务变成`SENDING`后退出，恢复后可以继续投递。

### 12.3 性能测试

- 使用实际数据量验证查询命中联合索引；
- 测试1万条工单同时到期时的分批处理速度；
- 观察扫描期间数据库CPU、连接池、慢查询和锁等待；
- 验证积压清空后空扫描的平均耗时。

## 13. 发布与回滚

### 13.1 发布步骤

1. 增加工单联合索引和站内消息幂等字段；
2. 发布通知投递、MQ confirm、消费幂等和`SENDING`恢复能力；
3. 将扫描任务改为分批处理，暂时保持5分钟周期；
4. 在测试环境验证查询计划和多实例并发；
5. 将周期灰度调整为60秒；
6. 观察数据库和通知指标后调整为30秒；
7. 根据实际负载决定是否继续调整。

### 13.2 回滚

如果数据库负载或通知异常：

1. 通过配置把扫描周期恢复为5分钟；
2. 保留联合索引和通知幂等能力；
3. 暂停失败渠道投递，不回滚已确认的工单超时状态；
4. 修复后重新处理`PENDING/RETRYING`投递记录。

方案不新增核心业务表，回滚不涉及定时事件数据迁移。

## 14. 工作量评估

### 必要改造，约2～3人日

- 增加联合索引；
- 扫描周期配置化；
- 分批查询和处理；
- 移除扫描任务`@Async`；
- 缩小事务范围；
- 完善单元测试。

### 通知可靠性增强，约2～3人日

- 复用投递表支持站内消息可靠发布；
- RabbitMQ Publisher Confirm和Returns；
- 站内消息消费幂等和死信处理；
- 飞书`SENDING`租约恢复；
- 指标、告警和故障测试。

完整实施预计4～6人日。如果本期只调整扫描周期、索引和批量处理，可压缩到2～3人日，但站内消息仍保留一定的丢失或重复风险。

## 15. 验收标准

1. 工单到期后30秒内被识别并更新为410；
2. 系统正常时，超时后1分钟内发起站内消息和飞书通知；
3. 扫描查询命中`idx_work_order_overdue`联合索引；
4. 每次查询和单轮处理量不超过配置上限；
5. 多实例同时扫描不产生重复状态更新和重复投递记录；
6. 单条工单异常不回滚整批处理；
7. RabbitMQ重投不产生重复站内消息；
8. 飞书失败不影响超时状态，卡住任务可以自动恢复；
9. 扫描周期能够通过配置在30秒、60秒和5分钟之间调整；
10. 数据库压力异常时可以仅调整配置快速回退。

## 16. 最终决策

本期采用“缩短并优化现有扫描”的方案：

```text
30秒扫描工单主表
+ 联合索引
+ 固定批量
+ 条件更新
+ 单工单短事务
+ 复用notification_delivery
+ RabbitMQ可靠通知
+ 5～10秒通知投递
```

该方案没有新建定时事件表，长期截止时间仍保存在工单表中。扫描频率虽然提高，但每次只读取联合索引中的少量到期记录，预计不会对当前低TPS项目造成明显压力。如果未来工单量、定时动作数量或可靠性要求显著提高，再演进为独立定时事件表或专业调度平台。
