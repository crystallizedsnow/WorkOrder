# 工单项目六大模块面试回答

> 回答口径：以下内容以当前仓库源码为准。面试时我会先讲主链路，再讲安全边界和设计取舍；对于“目标设计”和“当前实现”存在差异的地方，我会主动说明。

## 一、整个系统的鉴权是怎么做的？

### 1. 总体思路

我把鉴权拆成了三层：

1. **backend 是身份与权限的最终权威方**，负责登录、Token 签发、刷新、撤销、用户状态和角色校验。
2. **AiAssistant 不自己解析或信任 JWT**，而是拿请求中的 Bearer Access Token 回 backend 校验，得到真实的 `userId`，然后把同一个用户凭证隔离注入 CLI 子进程。
3. **飞书账号不能直接等同于工单账号**，需要先做外部身份绑定，再由 backend 换发短时 `channel_proxy` Token。后续业务链路仍然使用工单用户身份，不直接信任飞书的 `openId`。

这样设计的核心是：所有入口最终归一成一个可信的工单用户身份，鉴权规则只在 backend 收口，AiAssistant、CLI 和飞书渠道都不能自行扩大权限。

### 2. backend 的登录与 Token 生命周期

用户用手机号和密码登录。密码使用 BCrypt，强度参数是 12；为了兼容旧数据，历史明文密码在首次成功登录后会升级成 BCrypt 哈希。

登录成功后，backend 同时签发：

- **短期 Access Token**：JWT，使用 HS256 签名，包含 `sub`、`jti`、`iss`、`aud`、`sid`、`ver`、签发时间和过期时间。Token 只保存稳定身份声明，不把可变业务权限快照塞进去。
- **长期 Refresh Token**：32 字节安全随机数。数据库只保存 SHA-256 哈希，不保存明文 Refresh Token。

Refresh Token 使用 rotation：每次刷新时旧 Token 都会被原子标记为已使用，并签发同一 `familyId` 下的新会话。如果旧 Refresh Token 再次出现，系统认为可能发生重放，撤销整个 Token family。并发刷新也通过条件更新保证只能有一个请求成功。

Access Token 校验不只是验签和过期时间，还会检查：

- JWT 的 issuer、audience 和签名；
- 用户是否存在且状态正常；
- Token 中的 `ver` 是否等于用户当前 `authVersion`；
- 普通 Web/CLI Token 对应的 refresh session 是否存在、未撤销且未过期；
- 如果是渠道代理 Token，还要校验绑定记录、绑定状态和 `bindingVersion`。

管理员禁用用户、修改敏感身份信息或执行全端退出时，可以递增 `authVersion` 并撤销 Refresh Session，使已发出的 Access Token 在下一次校验时立即失效，而不必等 JWT 自然过期。

Spring Security 使用无状态会话。`JwtAuthenticationFilter` 校验 Bearer Token 后，把 Staff 和 `ROLE_xxx` 放入 SecurityContext；普通接口要求认证，管理员接口再用 `@PreAuthorize("hasRole('ADMIN')")` 做方法级授权。

Web 端还有一层浏览器适配：Access Token 返回给前端并保存在内存中；Refresh Token 放在 HttpOnly、SameSite Cookie 中，前端 JavaScript 不能读取。页面刷新时通过 Cookie 换取新的 Access Token，退出时撤销 refresh family 并清 Cookie。

### 3. AiAssistant 的鉴权

Web 调用 `/assistant/chat` 时必须携带 `Authorization: Bearer <access-token>`。AiAssistant 不把前端传入的 `memoryId` 当成用户身份，而是调用 backend 的 `/api/auth/validate` 校验 Token并解析出 `userId`，然后构造统一的 `AgentRequest(sessionId, userId, query, accessToken, channel...)`。

进入记忆模块时还会校验 `sessionId` 的 owner 是否等于当前 `userId`，防止用户通过猜测会话 ID 读取他人的历史。清除记忆也使用同样的 `sessionId + userId` 条件。

工具执行时，AiAssistant 不允许模型读取本地账号文件、执行 login/refresh，或在命令中传 `--password`、`--token`。当前请求的 Access Token 由服务端通过子进程环境变量 `WORKORDER_TOKEN` 注入 Go CLI，不出现在 Prompt、工具参数或日志命令中。CLI-Service 收到 Token 后再次调用 backend 校验，因此 Agent 层不是最终安全边界。

### 4. 飞书账号绑定和换票

飞书绑定采用“工单侧发起挑战、飞书侧完成确认”的方式：

1. 用户先以已登录的工单身份申请绑定码。backend 生成 `challengeId + 随机 secret`，数据库只保存 secret 的 SHA-256 哈希，并设置 5 分钟有效期、最大尝试次数和一次性状态。
2. 用户把绑定码发给飞书机器人。AiAssistant 先用内部服务密钥调用 backend，携带飞书的 `tenantKey + unionId + openId` 确认绑定。
3. backend 校验绑定码的状态、有效期、哈希和尝试次数，并保证“一个有效飞书身份只绑定一个工单用户，一个工单用户只保留一个有效飞书身份”。绑定关系保存在 `external_identity_binding`。
4. 后续每条飞书消息到达时，AiAssistant 先按 `tenantKey + unionId` 查询绑定，再向 backend 换取一个短时 `channel_proxy` JWT。
5. 该 Token 包含 `typ=channel_proxy`、`src=feishu`、`bid`、`bver`、`csid` 和用户 `ver`。backend 每次校验都会回查绑定状态和版本，因此解绑、换绑、禁用账号后，旧渠道 Token 会立即失效。

`openId` 主要用于当前应用内发消息，`unionId` 才作为租户内稳定绑定标识；内部的 find、confirm、exchange 接口还要求 `X-Workorder-Service-Key`，避免外部用户伪造渠道身份直接换票。

此外，飞书入口还有消息 ID 去重、按用户限流、同会话串行执行。它们不是身份认证本身，但可以防止平台重投或并发事件造成重复写操作。

### 5. 我如何概括这套鉴权

一句话概括：**backend 统一签发和验证身份，AiAssistant 只做可信身份解析与透传，飞书通过绑定换票转成短时工单身份；用户版本、会话版本和绑定版本共同提供可撤销性。**

---

## 二、ReAct 循环是什么过程？系统提示词、工具调用、任务规划和执行怎么做？

### 1. 一次请求进入 Agent 后的完整流程

我的 ReAct 不是依赖框架黑盒，而是在 `AgentLoop` 里写了显式循环，默认最多 10 轮：

1. 根据当前问题和最近两条用户消息生成检索 query。
2. 执行 RAG 检索，取得知识库上下文。
3. 从 MongoDB 加载当前 session 的摘要和最近完整消息，并按 Token 预算决定是否压缩。
4. 动态生成系统提示词，取得所有工具的 Function Calling JSON Schema。
5. 将“系统提示词 → 历史摘要 → RAG → 最近历史 → 当前问题”组成 messages，连同 tools 发给 LLM。
6. 如果模型返回 `tool_calls`，系统先保存 assistant 的工具调用消息，再按工具名分发执行。
7. 每个结果包装成 `role=tool`，并带上原 `tool_call_id`、工具名、`success` 和 `result`，进入下一轮，让模型基于 Observation 继续推理。
8. 如果模型没有返回工具调用，就把标签内推理剥离，校验和渲染 RAG 引用，保存稳定会话历史并返回最终答案。
9. 超过最大轮数仍未完成，就把会话记为 `INTERRUPTED`，停止继续调用，避免无限循环和费用失控。

这就是典型的 Reason → Act → Observe → Reason，只是“Act”使用模型原生 Function Calling，不从自然语言里用正则猜命令。

### 2. 系统提示词包含什么

系统提示词由固定模板和运行时信息组合，主要包含：

- **角色定位**：专业的工单客服 Agent，负责查询和管理工单；
- **知识来源约束**：流程和枚举知识可来自 RAG，实时业务状态必须调用工具；
- **工具目录**：工具名、描述和参数 JSON Schema；
- **Skill 元数据和 Skill 操作规程**；
- **ReAct 协议**：只能通过 `tool_calls` 发起工具，参数必须是合法 JSON，工具结果以 `role=tool` 返回；
- **长任务 Todo 规则**：多阶段、存在依赖的任务要先拆成至少两个可验证阶段；
- **写操作纪律**：按 Skill 执行 discover、schema、contract、dry-run、verify、wait-confirm、execute；
- **失败恢复**：401 立即停止，参数错误重新查 Schema，网络超时和服务端错误按上限重试；
- **凭证安全**：模型不得登录、刷新、读取凭证文件或在命令里携带密码/Token；
- **输出格式**：内部分析放在 `<thinking>` 标签中，系统剥离后只把标签外答案返回用户。

RAG 和历史摘要不是固定 Prompt 的一部分，而是独立 system message。摘要会明确标记为“历史线索，不是当前事实”，避免模型把历史工单状态或旧授权当成实时状态。

### 3. 工具是怎么注册和调用的

工具方法使用自定义 `@Tool` 和 `@P` 注解。应用启动时，`ToolDispatcher` 反射扫描工具 Bean，把 Java 方法转换成 Function Calling 定义：

```json
{
  "name": "executeCliCommand",
  "description": "执行 CLI 命令",
  "parameters": {
    "type": "object",
    "properties": {
      "CLI命令字符串": { "type": "string" }
    },
    "required": ["CLI命令字符串"]
  }
}
```

LLM 返回结构化 `tool_calls.function.name` 和 JSON `arguments` 后，Dispatcher 根据工具名查 Handler，完成参数解析、类型转换和反射调用。`sessionId` 这类系统参数不会暴露给模型，而是由 SessionContext 自动注入。

当前主要工具分两组：

- 工单工具：列出 CLI 能力、查询 dataCode Schema、执行 CLI、加载 Skill；
- Todo 工具：创建、查询、更新和清除 Agent 自己的任务计划。

工具结果不会被当成普通自然语言拼接，而是保留 Function Calling 协议字段，这能避免调用链错位，也便于审计。

### 4. 如何完成任务规划和执行

单一查询或单一操作不强制建 Todo，避免过度规划。对于包含三个及以上动作且存在“先后、依赖、上一步产生下一个输入”的长任务，AgentLoop 会设置硬门禁：如果模型在调用业务工具前没有创建至少两个 Todo，本轮业务工具就不执行，而是追加系统门禁消息让模型重新规划。

Todo 的状态是 `pending → in_progress → completed`。开始一个阶段前更新为 `in_progress`，拿到满足阶段目标的结果后更新为 `completed`；连续三轮没有更新 Todo 会收到系统提醒。Todo 负责计划和恢复进度，但不提供业务权限，也不能跨越 Skill 里的用户确认点。

例如“查询故障工单，选择一张分配给张三，成功后告诉我结果”：

1. Todo 1：查询并确定目标工单；
2. Todo 2：查询运行时 Schema 并生成分配预演；
3. Todo 3：等待用户确认后执行分配并返回结果。

前两个阶段执行完后必须停在确认点，不能因为还有未完成 Todo 就替用户确认。确认消息会被 Channel 层截获，不再进入 ReAct；确认后执行一次并停止。

### 5. 循环如何防止失控

- 最大 10 轮；
- 对 `工具名 + 规范化参数` 做指纹，相同调用在一次请求内只执行一次；
- 禁止凭证类工具调用；
- 工具结果设置 Token 上限，过大结果保留结构和预览；
- 错误先分类，可恢复错误有限重试，不可恢复错误直接降级；
- 写操作最终由 CLI-Service 门禁，不依赖模型是否遵循提示词；
- Hook 记录 session、模型响应、工具调用和 Token 统计，便于追踪。

---

## 三、记忆模块怎么实现？

### 1. 定位与底层存储

当前实现的是**会话级短期记忆**，不是跨 session 的用户画像。底层使用 MongoDB，集合名是 `sessionMemories`。一个 session 对应一个文档，主要字段有：

- `sessionId`、`workorderUserId`：会话和所属工单用户；
- `summary`、`summaryThroughSequence`、`summaryVersion`：滚动摘要和覆盖位置；
- `recentMessages`：最近完整消息，保留 role、content、toolCalls、toolCallId 和 tool name；
- `nextSequence`：消息稳定序号；
- `version`：乐观锁版本；
- `state`：ACTIVE 或 INTERRUPTED；
- `lastRequestId`：请求幂等；
- `createdAt`、`updatedAt`、`expiresAt`、`lastCompressedAt`。

MongoDB 上有 `sessionId` 唯一索引、`workorderUserId + updatedAt` 索引和 `expiresAt` TTL 索引。默认会话保留 30 天，每次使用会刷新 TTL。保存时使用 `sessionId + userId + version` 条件更新并递增 version，冲突最多重试 3 次。

选择 MongoDB 的原因是消息和 Function Calling 都是嵌套结构，字段还会演进；上下文加载本身也是按 session 整体读取，文档模型比拆成多张关系表更自然。

### 2. 跨回合需要传给 LLM 什么

下一回合不会把数据库文档原样全部传给模型，而是按以下顺序组装：

1. 当前动态系统提示词；
2. 历史滚动摘要，单独标注“仅作历史线索”；
3. 当前问题命中的 RAG 上下文；
4. 最近完整的 user/assistant/tool 原子单元；
5. 当前用户消息；
6. 本轮可用工具定义。

不会重复持久化和回传动态系统 Prompt、RAG 内容、运行期提醒；也不会持久化含 Bearer、密码等敏感内容。这样既避免上下文膨胀，也防止临时检索结果被固化成永久事实。

### 3. 什么时候触发压缩

每次调用模型前都会估算：

```text
系统提示词 + RAG + 旧摘要 + 最近历史 + 当前问题
+ 工具定义 + 模型输出预留 + 安全余量
```

只有预计总量达到 `compression-trigger` 才压缩。当前配置按 16K 上下文设计，仓库运行配置中触发值是 15000，输出预留 1024、安全余量 128；这些值都可通过环境变量调整。这里我会强调：面试时不能只背类里的默认值，因为实际生效值以 `application.yaml` 为准。

### 4. 怎么压缩

首先由 `MessageUnitSegmenter` 把历史切成不可拆分的原子单元：普通 user/assistant 一轮，或者 user → assistant tool_calls → 全部 tool results → 最终 assistant 的完整工具轮次。存在未匹配 tool call 的不完整单元不会进入压缩区。

系统从最旧的完整单元开始选择，直到最近历史降到目标预算，然后把“旧摘要 + 本次选中的新历史”交给 LLM 生成滚动摘要。摘要格式固定为：

- 当前目标；
- 明确约束；
- 已完成事项；
- 关键业务标识；
- 未完成事项；
- 失败与待补充。

压缩器还会做两类校验：摘要不能包含凭证；摘要中的工单号必须来自原文，不能被改写、补全或虚构。摘要超过预算或校验失败时，保留旧摘要和原消息，采用 fail-safe，不做破坏性替换。成功后才原子保存新摘要，并删除已经被摘要覆盖的 recentMessages。

### 5. 如何查询某个人某个 session 的会话

这里需要先纠正问题：**实际会话存储是 MongoDB，不是关系数据库，所以生产查询不写 SQL。** 等价的 mongosh 查询是：

```javascript
db.sessionMemories.findOne(
  {
    workorderUserId: "12345",
    sessionId: NumberLong("88")
  },
  {
    _id: 0,
    sessionId: 1,
    workorderUserId: 1,
    summary: 1,
    summaryThroughSequence: 1,
    recentMessages: 1,
    state: 1,
    updatedAt: 1,
    expiresAt: 1
  }
)
```

如果只想展开消息，可以写：

```javascript
db.sessionMemories.aggregate([
  {
    $match: {
      workorderUserId: "12345",
      sessionId: NumberLong("88")
    }
  },
  { $unwind: "$recentMessages" },
  { $sort: { "recentMessages.sequence": 1 } },
  {
    $project: {
      _id: 0,
      sequence: "$recentMessages.sequence",
      role: "$recentMessages.message.role",
      content: "$recentMessages.message.content",
      toolCalls: "$recentMessages.message.toolCalls",
      toolCallId: "$recentMessages.message.toolCallId",
      toolName: "$recentMessages.message.name"
    }
  }
])
```

需要注意，已经滚入摘要的原始消息会从 `recentMessages` 删除，所以查询结果是“摘要 + 未压缩最近历史”，不是永久审计日志。如果业务要求全量审计，应该另建 append-only 会话审计库，不能让短期记忆同时承担审计职责。

### 6. 为什么没做长期记忆

我不是因为做不到，而是因为当前收益不足以覆盖安全和治理成本。旧方案存在这些问题：

- 把 sessionId 当 userId，身份边界不可信；
- 用关键词抽取偏好，容易把临时要求、助手猜测或工具结果误记成用户事实；
- 本地文件不支持多实例共享和并发更新；
- 缺少 TTL、删除权、审计、敏感信息识别和用户授权；
- 工单状态是实时事实，跨会话记忆很容易变成过期信息；
- 一旦错误长期记忆进入 Prompt，会在多个 session 持续污染回答。

所以当前只做 30 天会话记忆。以后如果要做长期记忆，我会先定义允许记忆的数据类型、显式用户同意、可查看/修改/删除、来源和置信度、过期策略、敏感信息过滤及离线评测，再考虑向量检索，而不是简单把聊天内容永久保存。

### 7. 如何评估压缩是否保留关键信息

我会建立“原始完整上下文”和“压缩上下文”的成对测试集，覆盖工单号、用户约束、多工具链、失败恢复、待确认写操作、指代追问和恶意凭证内容。主要看五组指标：

1. **事实保留率**：关键工单号、flowId、人员、时间、约束值的 Precision/Recall/F1；关键标识要求 Recall 100%。
2. **任务状态准确率**：已完成、未完成、失败、待补充是否分类正确；Todo 恢复准确率和下一步选择准确率。
3. **工具链完整率**：保留的 tool call/result 配对率应为 100%，非法孤立 tool 消息为 0。
4. **下游任务一致性**：同一问题分别使用原上下文和压缩上下文，比较答案正确率、工具选择一致率、参数一致率、写操作误触发率。写操作旧确认复用率必须为 0。
5. **安全与效率**：凭证泄露率 0、幻觉新增事实率 0，同时观察压缩率、输入 Token 降幅、P95 延迟和压缩失败率。

自动指标之外，还要对高风险样本做人工评分：目标、约束、关键标识、完成状态、未完成事项各 0～2 分。最终不是单纯追求摘要越短越好，而是在安全红线不下降的前提下减少 Token。

---

## 四、工单 Skill 如何加载、识别和处理错误？

### 1. Skill 的功能边界

我把 Skill 定义成“业务操作规程”，而不是执行器。它负责描述：

- 这个能力适用于什么请求、不适用于什么请求；
- 允许使用哪些工具；
- 运行时如何发现 dataCode 和 Schema；
- 读操作、写操作分别按什么状态机执行；
- 何时必须 dry-run、何时停下来等待用户确认；
- 参数丢失、401/403/404、超时和服务端错误怎么恢复；
- 最终结果应如何展示。

Skill 不负责签发身份、不直接访问数据库、不绕过 CLI-Service，也不拥有最终业务校验权。权限归 backend，写安全门禁归 CLI-Service，Skill 只是指导 Agent 正确编排工具。

### 2. Skill 怎么被加载

应用启动时，`SkillLoader` 扫描 `classpath:skills/**/SKILL.md`，读取 Markdown 和 YAML 风格 frontmatter，解析 key、name、description、version、capabilities、正反例、allowedTools、riskLevel、requiresConfirmation、priority 等元数据，并按目录名注册。

`SkillRegistry` 会进一步校验：

- 必填元数据是否完整；
- riskLevel 是否为 READ、WRITE 或 SENSITIVE；
- allowedTools 是否真的已经注册；
- key 是否重复；
- conflictsWith 是否引用有效 Skill。

校验失败会让应用启动失败，避免一个不完整或引用未知工具的 Skill 悄悄上线。注册表内容还会生成版本哈希，便于路由审计和后续热更新。

### 3. Skill 怎么被识别

当前有两条识别路径：

1. 主 Agent 的系统提示词包含 Skill 的名称、描述和元数据，模型根据用户意图匹配 Skill，并可调用 `loadSkill(skillName)` 获取全文。
2. 新的意图路由模块会把 Skill 的 capabilities、description 和正负样例向量化，计算用户 query 与正例、负例的相似度，形成候选 Skill；再结合域内/域外判断和策略校验。

但我要诚实说明当前状态：意图路由配置还是 `SHADOW`，只生成和审计路由决策，不拦截主链路，也没有据此裁剪工具集；而且 `DynamicPromptGenerator.buildSkillContentSection()` 当前会把已扫描 Skill 全文都注入系统提示词。因此现在具备“运行时 loadSkill 接口”和“动态扫描注册”，但还没有完全做到严格的按需全文加载。下一步应让路由结果成为 `PromptBuilder` 和 `ToolDispatcher` 的白名单输入，只注入命中 Skill 全文及其 allowedTools。

### 4. Skill 的错误处理设计

错误处理分三层：

- **加载期**：单个文件读取/解析异常记录明确日志；注册信息不完整、重复 key、未知工具等结构性错误 fail-fast，阻止错误 Skill 发布。
- **Agent 执行期**：未知 Skill 返回“技能不存在”；未知工具、参数 JSON 错误、类型转换失败都包装成 `success=false` 的工具结果，让模型依据 Skill 恢复，不能把失败当成功。
- **服务端安全层**：即使模型和 Skill 都出错，CLI-Service 仍强制检查 previewId、用户、dataCode、Schema 版本、参数摘要、确认状态和一次性消费状态。也就是说，提示词错误最多影响体验，不能越过写操作门禁。

工单 Skill 对常见错误有明确状态迁移：参数错误重新走 Schema/Contract；401 停止并要求调用方重新登录；403 停止报告权限不足；404 重新 Discover；网络超时按 1、2、4 秒退避且最多 3 次；服务端错误最多 2 次，业务校验错误不盲目重试。

### 5. 为什么选 CLI 而不是直接脚本

我选择编译后的 Go CLI，主要不是因为“脚本不能做”，而是因为 CLI 更适合成为多人和 Agent 共用的稳定产品边界：

- Cobra 提供明确的命令、flag、help、退出码和参数校验；
- `list/schema` 让 Agent 在运行时发现能力，不需要记住 REST 路径和 DTO；
- 编译成单文件，可跨环境发布，运行时依赖少；
- 人可以在终端复现 Agent 的同一条命令，排障和审计更直观；
- 输出统一 JSON，Agent 不需要解析随意的 stdout；
- Token 注入、超时、TraceId、读写入口和 dry-run 协议可以集中实现；
- CLI-Service 隔离了后端接口变化，Agent 只依赖稳定 dataCode。

如果用 Python/PowerShell/Shell 脚本，我也会保持相同架构，而不是写一堆散落脚本：

1. 建一个单一入口，例如 `workorder.py`；
2. 用 argparse/Typer 定义 `list`、`schema`、业务 dataCode、`--dry-run`、`--preview-id`；
3. 从环境变量读取短时 Token，禁止凭证出现在命令参数；
4. 所有请求统一转换成 `{dataCode, params}` 调 CLI-Service；
5. stdout 只输出稳定 JSON，日志写 stderr，定义固定退出码；
6. 使用 JSON Schema 校验参数，写操作仍由 CLI-Service 生成和消费 previewId；
7. 打包成固定版本的 zipapp/PEX 或容器镜像，并做契约测试和签名校验。

脚本方案迭代更快，但解释器版本、依赖环境、源码篡改、跨平台引用和输出稳定性更难治理。当前 Go CLI 的工程成本是值得的。

---

## 五、预演机制怎么实现？记忆压缩会导致预演丢失吗？

### 1. 预演链路

写操作必须走以下状态机：

```text
DISCOVER → SCHEMA → EXPECT → CONTRACT
→ PREVIEW → VERIFY → WAIT_CONFIRM
→ CONFIRMED → EXECUTING → SUCCEEDED/FAILED
```

Agent 先通过 `workorder-cli list` 和 `schema <dataCode>` 获取实时能力与参数契约，然后执行：

```text
workorder-cli --dry-run <dataCode> <flags/body>
```

Go CLI 会把命令转为 `dataCode + params`，调用 CLI-Service 创建预演。CLI-Service 根据运行时 Schema 和预演元数据生成可读计划，包括操作名、HTTP 方法、目标端点、规范化参数、风险、影响和有效期。

随后 CLI-Service 生成：

- `previewId`；
- 规范化后的参数 `canonicalParams`；
- `requestDigest = hash(userId + dataCode + schemaVersion + canonicalParams)`；
- 初始状态 `PREVIEWED`；
- 默认 10 分钟 TTL。

这条预演记录保存在 Redis，key 是 `workorder:write-preview:<previewId>`。

### 2. 用户确认与正式执行

AiAssistant 看到 dry-run 输出中的 previewId 后，会在 MongoDB 的 `channel_write_confirmations` 集合中创建待确认记录，绑定 `sessionId + userId + conversation + requester + previewId + commandDigest + expiresAt`。Web 使用严格 JSON 确认协议，飞书使用交互卡片；确认请求在 Channel/Gateway 层直接拦截，不进入 ReAct。

确认时先校验操作仍为 PENDING、未过期、确认人和原用户/原会话一致，然后调用 CLI-Service 把 Redis 状态从 `PREVIEWED` 原子改成 `CONFIRMED`。正式命令不从聊天历史重建，而是使用服务端记录的 previewId。

CLI-Service 执行前再次检查：

- 当前用户是否等于创建预演的用户；
- dataCode 是否一致；
- 当前参数摘要是否等于预演摘要；
- Schema 版本是否一致；
- 状态是否为 CONFIRMED；
- 能否从 CONFIRMED 原子占用成 EXECUTING。

任何一项不一致都不会进入真实 WriteService。执行完成后状态改为 SUCCEEDED 或 FAILED；已经消费、取消、过期的 preview 不能重放。

### 3. 记忆压缩会不会导致预演丢失

**不会导致安全凭证丢失，也不会导致系统绕过确认。** 原因是预演的权威状态不在 LLM 上下文里：

- previewId、规范化参数、摘要、状态和 TTL 在 CLI-Service 的 Redis；
- 待确认操作及原用户、原 session、原会话在 AiAssistant 的 MongoDB；
- 确认消息由 Channel 层按数据库最新 PENDING 记录处理，不要求模型回忆 dry-run 命令；
- 正式执行必须带服务端绑定的 previewId，并重新通过摘要和状态校验。

即使对话里的 dry-run 文本被滚入摘要，最坏结果也是用户看不到旧预演细节、需要重新预演，不能变成未确认执行。并且压缩只处理完整工具单元，不会把 tool call 和 tool result 拆开。

这里还有一个重要安全原则：摘要不能保存或恢复“历史写操作授权”。用户确认只对某个短时 previewId 有效，不能通过一句“用户之前同意过”跨回合复用。

当前 Redis 的状态转换用 JVM `synchronized`，适合单实例 CLI-Service；多实例部署时要替换成 Redis Lua 或 WATCH/MULTI 的 CAS，这是我会主动说明的演进点。

---

## 六、知识库怎么实现？

### 1. 整体链路

知识内容以 Markdown 文件维护，并通过 `manifest.properties` 建立可信来源白名单。构建过程是：

```text
读取 manifest
→ 校验来源、版本、owner、可信等级和文件唯一性
→ 加载 Markdown
→ 标题感知切片
→ 本地 ONNX 模型生成向量
→ Bulk 写入新的 Elasticsearch 物理索引
→ 校验文档数量
→ 原子切换 alias
→ 保留旧版本用于回滚
```

构建失败不会清空旧索引；应用在 embedding 或 ES 不可用时降级为不检索，保证主服务仍能启动和使用实时工具。

### 2. Embedding 模型选型

当前使用 **BAAI/bge-small-zh-v1.5**，本地 ONNX 推理，512 维，最大分词长度 512。Java 侧通过 DJL、HuggingFace Tokenizer 和 ONNX Runtime 加载，自定义 Translator 对 `last_hidden_state` 按 attention mask 做 mean pooling，再做 L2 归一化。

选择它的原因：

- 工单知识主要是中文，BGE 中文语义能力比通用英文小模型更合适；
- small 版本在召回效果、CPU 时延和内存之间平衡较好；
- 本地推理不产生外部 Embedding API 成本，减少网络时延和内部文档外发风险；
- 512 维能降低 ES 存储和 KNN 计算成本；
- ONNX 便于固定模型版本和离线部署。

它的代价是需要管理模型文件、Java ONNX 运行时和 CPU 容量；模型变更时必须用新索引重建，不能把不同维度向量混写。

### 3. 怎么分片

切分器是标题感知的 Markdown Splitter：

- 解析 `#` 到 `######` 的标题层级，保留 `headingPath`；
- 空行形成自然 block，表格等连续文本尽量不被拆散；
- 标题变化或合并后超过阈值时输出片段；
- 默认每片最大约 1200 字符，重叠 160 字符；
- 每片带 source、sourceName、sourceVersion、headingPath、trustLevel 和 ordinal；
- 用“来源 + 版本 + 标题路径 + 序号 + 正文”的 SHA-256 生成稳定 chunk ID。

标题路径会拼进片段正文，同时 ES 的 lexical query 对 `headingPath` 加更高权重，因此“状态含义”“流程步骤”这类标题信息不会在切片后丢失。

### 4. Query 有没有重写

有，但当前是轻量、确定性的上下文化，不再调用一个生成模型重写。`QueryContextualizer` 只对长度较短或含“那、这个、它、该、上一个、下一步、呢、怎么办”等指代的追问触发，取最近两条用户消息中最近的有效问题，改成：

```text
上文问题：……
当前追问：……
```

改写后的 query 只用于检索，原始问题仍交给最终回答模型。这样能解决“那下一步呢”之类多轮省略，又减少过度改写改变用户意图的风险。

### 5. 怎么召回

召回采用 Elasticsearch 双路混合检索：

1. **BM25/关键词路**：在 `text^2`、`headingPath^3`、`sourceName` 上做 multi-match，适合状态码、专有名词和精确枚举；
2. **KNN/向量路**：query 经同一 BGE 模型得到 512 维向量，在 `embedding` 字段搜索，并按 `minScore` 过滤；
3. 两路各取候选，当前配置 candidate 为 20；
4. 使用 Reciprocal Rank Fusion，`1 / (60 + rank)` 融合并按稳定 chunk ID 去重；
5. AUTHORITATIVE 来源有轻量 tie-break 优先；
6. 最终最多返回 5 个片段，并受 1800 Token 的 RAG 上下文预算约束。

只有 manifest 中标记为 AUTHORITATIVE 或 REVIEWED 且可引用的内容可以参与最终召回。答案生成后，`CitationService` 会校验模型引用是否真的来自本次召回，并由服务端渲染来源，防止模型伪造引用。

### 6. 知识库内容有什么

当前 manifest 真正发布的知识源有四类：

- `workorder-enums`：后端权威工单枚举，包括状态、类型、优先级等；
- `work-order-process`：工单处理流程；
- `system-overview`：系统总体介绍；
- `response-format`：面向用户的信息展示规范。

目录里虽然还有其他 Markdown 文件，但没有进入 manifest 的文件不会被可信导入器发布。实时工单内容、当前处理人、当前状态不放知识库，必须调用 CLI/backend 查询；RAG 负责相对稳定的说明性知识，不替代事实库。

### 7. 怎么存储和发布

Elasticsearch 每个 chunk 保存：

- `embedding` dense_vector；
- `text`；
- `source`、`sourceName`、`sourceVersion`；
- `headingPath`；
- `trustLevel`；
- `buildVersion`。

每次构建创建 `workorder-kb-v<version>-<timestamp>` 新物理索引，Bulk 成功且 count 校验一致后，再把 `workorder-kb-current` alias 原子切过去。新版本失败时旧 alias 不动；系统保留旧索引，支持健康检查、重建和回滚。

### 8. 怎么评估

当前代码提供基于 `rag-golden-set.tsv` 的离线评测：问题命中预期 source 就计为 hit，输出 Recall@K 和未命中问题。完整评测我会分为四层：

1. **检索质量**：Recall@5、MRR、nDCG@K、精确枚举 Recall@3、空召回率；
2. **证据质量**：可信来源正确率、片段相关性、重复片段率、引用覆盖率、伪造引用放行率；
3. **答案质量**：Faithfulness/事实一致性、Answer Relevance、无依据问题拒答率、实时事实是否正确转工具查询；
4. **工程指标**：构建成功率、重复构建文档增长率、索引切换正确率、P95 检索时延、embedding/ES 降级率。

当前方案建议的发布门禁是：Recall@5 不低于 90%，精确枚举 Recall@3 为 100%，可信来源正确率 100%，使用 RAG 的答案引用覆盖率 100%，伪造引用放行率 0%，无依据问题错误作答率不高于 5%，重复构建文档增长率为 0。

---

## 七、意图识别怎么实现？

### 1. 为什么单独做意图识别

我把意图识别放在 ReAct 主循环之前，目标不是替代 Agent 推理，而是提前回答三个问题：

1. 用户请求是否属于当前工单系统的能力范围；
2. 应该进入系统帮助、知识问答、业务 Skill 执行，还是要求用户补充信息；
3. 如果需要执行 Skill，允许加载哪些 Skill、暴露哪些工具。

这样可以避免把“是否属于工单域、选择什么 Skill、怎么执行业务”全部压给主 Agent，也为后续按意图裁剪 Prompt 和工具集合建立服务端白名单。

### 2. 当前完整识别链路

当前链路可以概括为：

```text
用户请求
→ 控制协议识别
→ Skill 语义候选召回
→ 意图模型结构化分类
→ 服务端策略校验
→ 生成 RoutingDecision
→ Shadow 审计
→ ReAct 主链路继续执行
```

`DefaultAgentGateway` 在完成写确认分流后调用 `IntentShadowService.observe()`。识别任务进入独立线程池异步执行，不阻塞用户当前的 ReAct 请求；线程池满时直接记录 `queue_rejected` 并跳过，不能拖垮主链路。

第一步先识别控制协议。如果消息是：

```json
{"action":"confirm_execute","target":"last_dry_run","confirmed":true}
```

或者取消协议，就直接判为 `CONTROL_CONTINUATION`，不拿确认文本参与普通业务意图分类。实际确认在更前面的 `WriteConfirmationCoordinator` 已经被拦截；这里的控制识别主要用于路由观测和协议完整性。

### 3. Skill 候选是怎么召回的

候选召回使用 `SemanticSkillRetriever`。它不是直接拿整份 SKILL.md 做向量，而是为每张 Skill Card 建立三组语义证据：

- Skill 的 `description + capabilities`；
- `positiveExamples` 正例；
- `negativeExamples` 负例。

同时，`DomainScopeRegistry` 从 `intent/domain-scope.json` 加载当前系统的领域描述、支持主题、正例和负例，例如“查询处理中工单”是域内，“天气、写 Java 代码、餐厅推荐、写工单主题诗歌”是域外。

这些文本使用与 RAG 相同的本地 EmbeddingModel 向量化。查询到来后计算余弦相似度：

```text
positiveScore = query 与 Skill 正向语义证据的最大相似度
negativeScore = query 与 Skill 负例的最大相似度
scoreMargin   = positiveScore - negativeScore
```

候选当前按 `positiveScore` 降序排列，最多取 Top 3；`negativeScore` 和 `scoreMargin` 会进入候选证据与审计，但目前还没有参与 Top K 排序或服务端阈值过滤。负例本身还会随 Skill Card 交给分类模型，用来区分“出现了工单两个字”和“真的请求系统支持的工单能力”，例如“写一首关于工单的诗”虽然包含领域词，但属于 Skill 的负例。后续可以在离线评测校准后，把 margin 阈值加入确定性召回策略。

如果本地 Embedding 模型不可用，召回器不会阻断请求，而是退化为返回注册表中有限数量的 Skill 候选，并标记 `embeddingAvailable=false`。这保证意图识别故障不会影响核心问答链路，但降级结果只能用于观测，不能被当作高置信度权限依据。

### 4. 意图模型怎么分类

候选召回后，`IntentModelClient` 调用一个独立配置的意图模型。当前可以与主 Agent 使用同一个模型名，但温度、最大输出、超时和 thinking 配置独立：温度为 0、最多 512 tokens、超时 15 秒、关闭 thinking，使分类结果尽量稳定且低成本。

送给分类模型的不是全部系统工具，而是：

- 原始 `userQuery`；
- 当前工单域的名称、说明、支持主题和负例；
- Top K 候选 Skill 的 key、description、capabilities、正负例和 riskLevel。

分类模型被明确告知：它只是入口分类器，不是客服助手；用户文本只是待分类数据，不能覆盖系统指令；只能从候选 Skill 中选择，不能编造 Skill。

模型必须通过强制 Function Calling `emitRoutingDecision` 返回结构化结果，而不是自由文本。核心字段包括：

```json
{
  "routeType": "SKILL_EXECUTION",
  "intents": [
    {
      "skillKey": "workorder-query",
      "object": "work_order",
      "action": "query",
      "dependsOn": []
    }
  ],
  "missingInformation": [],
  "reasonCode": "MODEL_CLASSIFIED"
}
```

系统支持六类路由：

| RouteType | 含义 |
|---|---|
| `CONTROL_CONTINUATION` | 写操作确认、取消等控制协议 |
| `SYSTEM_HELP` | 问候、询问助手能力或使用方法 |
| `KNOWLEDGE_QA` | 当前工单系统的状态、权限、SLA、流程知识 |
| `SKILL_EXECUTION` | 需要执行一个或多个已注册 Skill |
| `CLARIFY` | 信息不足、意图含混，或支持与不支持的请求混在一起 |
| `OUT_OF_SCOPE` | 天气、通用编程、创作、预测等系统未注册能力 |

如果用户有多个受支持动作，模型会输出有序 intents，并用 `dependsOn` 表达依赖关系，为后续 Todo 规划和多 Skill 编排提供结构化输入。

### 5. 为什么模型结果不能直接执行

分类模型只提出 proposal，最终要经过 `IntentRoutingPolicy` 的确定性校验：

- 模型选择的 Skill 必须存在于当前 SkillRegistry；
- 还必须出现在本次语义召回候选中，防止模型越过候选白名单；
- 多个 Skill 不能违反 `conflictsWith`；
- `SKILL_EXECUTION` 必须至少包含一个合法 intent；
- allowedTools 只能从已通过校验的 Skill Card 的 `allowedTools` 合并得到；
- 缺失或非法选择统一降级为 `CLARIFY`，而不是猜测执行。

最终形成的 `RoutingDecision` 还包含 routeId、允许的 Skill 和工具集合、缺失信息、候选相似度、Skill 注册表版本、策略版本、Embedding 是否可用和耗时。这使每次路由都可追踪、可复现，也便于之后灰度升级策略。

### 6. 当前是否真正拦截 ReAct

当前配置是：

```yaml
workorder:
  intent-routing:
    enabled: true
    mode: SHADOW
```

因此它现在是**影子识别**：异步计算并记录路由结果，但不会阻止请求进入 Agent，也不会根据 `allowedSkillKeys/allowedToolNames` 裁剪系统提示词和工具集合。无论识别结果是 OUT_OF_SCOPE 还是 SKILL_EXECUTION，当前主 ReAct 行为都保持不变。

这样上线是为了先积累真实流量数据，观察误判、时延和降级情况，避免分类器刚上线就误拦截正常工单请求。Shadow 稳定以后，再切换到 ENFORCE，目标行为是：

- SYSTEM_HELP 直接走帮助回答；
- KNOWLEDGE_QA 只开放 RAG，不开放写工具；
- SKILL_EXECUTION 只注入命中 Skill 全文和它声明的 allowedTools；
- CLARIFY 只向用户追问缺失信息；
- OUT_OF_SCOPE 直接拒绝或提示能力边界。

这里需要强调：虽然配置枚举里已经定义了 `ENFORCE`，但当前 `IntentShadowService` 只在 `mode == SHADOW` 时运行，主 Gateway 也没有消费 RoutingDecision，所以强制模式尚未真正接入。这是下一阶段工作，不应在面试中说成已经落地。

### 7. 错误处理和降级

- 意图线程池满：跳过本次 Shadow，并记录 `queue_rejected`；
- Embedding 不可用：返回有限候选并标记降级；
- 模型超时、网络错误或结构化输出非法：记录 `INTENT-SHADOW-FAILED`，不影响主请求；
- 模型选择未知或未召回 Skill：策略改判 `CLARIFY`；
- Skill 注册信息不合法或 golden set 引用未知 Skill：启动时 fail-fast；
- 审计日志只记录 route、候选分数、版本和耗时，不记录 Access Token。

### 8. 怎么评估意图识别

仓库中维护了 `intent/golden-set.json`，覆盖系统帮助、知识问答、工单查询与写操作、域外请求、信息不足、混合意图和 Prompt Injection。启动时会校验每条 expectedRoute 和 expectedSkills 是否有效。

离线和 Shadow 评估我会重点看：

1. **路由分类指标**：整体 Accuracy、Macro-F1，以及每个 RouteType 的 Precision、Recall、F1；
2. **Skill 召回指标**：Candidate Recall@K、Top-1 Accuracy、MRR，确保正确 Skill 在进入分类模型前没有被漏掉；
3. **安全指标**：OUT_OF_SCOPE 误放行率、写 Skill 错选率、未授权工具暴露率、Prompt Injection 放行率；这些比总体准确率优先级更高；
4. **澄清质量**：该澄清时直接执行的比例，以及本可执行却过度澄清的比例；
5. **线上 Shadow 指标**：主 Agent 实际调用工具与路由 allowedTools 的一致率、人工抽样误判率、embedding 降级率、模型失败率、队列拒绝率和 P95 路由延迟；
6. **版本对比**：按 registryVersion、policyVersion 和模型版本切分报表，避免 Skill 更新后把指标变化错误归因给模型。

正式切到 ENFORCE 前，我会设置安全门禁，例如：写操作 Skill Recall 接近 100%、OUT_OF_SCOPE 高风险误放行为 0、未知工具放行为 0，并先按小流量灰度，只对低风险 SYSTEM_HELP/KNOWLEDGE_QA 启用强制路由，再逐步覆盖写操作。

### 9. 面试时的一句话总结

**我的意图识别采用“语义召回缩小候选、LLM 做结构化判定、服务端策略做白名单校验”的三段式架构；当前先以 Shadow 模式验证质量，不把生成模型的自报结果直接当作执行权限。**

---

## 八、适合面试结尾的总结

这个项目里我最重视的不是“让模型会调用接口”，而是把不确定的模型放在确定性的工程边界里：

- 身份由 backend 统一验证，Agent 不持有登录能力；
- ReAct 只做理解和编排，工具协议结构化、轮数和重试有上限；
- Skill 描述业务规程，但真正权限和写门禁仍由服务端保证；
- 会话记忆可以压缩，但工具调用链、关键标识和安全边界不能压缩掉；
- dry-run 和一次性 previewId 让用户确认与真实参数绑定；
- RAG 只回答稳定知识，实时业务事实回到 backend 查询。

所以这套系统的核心设计原则可以概括为：**LLM 负责决策建议，确定性服务负责身份、状态、参数一致性和副作用。**

## 九、主要代码依据

| 主题 | 主要实现 |
|---|---|
| backend 鉴权 | `AuthTokenService`、`TokenUtil`、`JwtAuthenticationFilter`、`SecurityConfig` |
| Web 登录 | `WebAuthController`、`LoginServiceImpl` |
| 飞书绑定 | `ChannelIdentityService`、`ChannelRouter`、`FeishuIdentityClient` |
| Agent 鉴权 | `AIChatController`、`BackendAuthenticatedUserResolver` |
| ReAct | `AgentLoop`、`ToolDispatcher`、`DynamicPromptGenerator` |
| Skill | `SkillLoader`、`SkillRegistry`、`SemanticSkillRetriever`、`skills/workorder-query/SKILL.md` |
| 意图识别 | `IntentShadowService`、`IntentShadowRouter`、`IntentModelClient`、`IntentRoutingPolicy`、`DomainScopeRegistry` |
| 记忆 | `SessionMemoryService`、`RollingSummaryCompressor`、`MessageUnitSegmenter`、`MongoSessionMemoryRepository` |
| 预演 | `WritePreviewManager`、`WriteExecutionGuard`、`RedisWritePreviewStore`、`WriteConfirmationService` |
| RAG | `KnowledgeBaseIngestor`、`DocumentSplitter`、`OnnxEmbeddingModel`、`ElasticsearchVectorStore`、`EmbeddingStoreContentRetriever` |
