# 延期工单飞书提醒实施与测试方案

## 已实现链路

1. 后端每 5 分钟扫描 `400/670` 且超过截止时间的工单，并通过条件更新抢占首次延期资格。
2. 查询所有 `handle_type=4、finished=0、deleted=0` 的处理人并去重。
3. 为每个处理人创建唯一 FEISHU 投递任务；重复扫描或重复入队由数据库唯一键拦截。
4. 投递调度器每 30 秒原子抢占最多 100 条任务，一次联表解析有效飞书绑定。
5. 未绑定、解绑、禁用或没有 openId 的用户记为 `SKIPPED_UNBOUND`。
6. AiAssistant 接收批量请求，使用有界线程池逐用户按 `open_id` 发送延期卡片。
7. 网络、限流和平台服务异常进入指数退避，最多 3 次；永久错误直接记为 `FAILED`。
8. 可在 `notification_delivery` 按 eventId/workOrderId/receiverId 查询状态、次数和错误。

## 启动前准备

1. 执行 `backend/workorder-server/src/main/resources/work_order_delayed_notification_migration.sql`。
2. 后端启动在 8080，AiAssistant 启动在 8081，RabbitMQ 正常运行。
3. 设置 `FEISHU_ENABLED=true`、`FEISHU_APP_ID`、`FEISHU_APP_SECRET`。
4. 确保至少一个状态正常的工单用户已绑定飞书且绑定表中 open_id 非空。

## 联调步骤

运行：

```powershell
cd D:\JavaCode\-WorkOrder\AiAssistant
python create_overdue_work_order.py
```

等待最多 6 分钟，然后检查：

```sql
SELECT id, code, status, deadline_time FROM work_order WHERE code LIKE 'WO-DELAY-%' ORDER BY id DESC LIMIT 1;
SELECT event_id, work_order_id, receiver_id, channel, status, attempts, last_error, sent_at
FROM notification_delivery ORDER BY id DESC LIMIT 10;
```

预期工单状态为 410，已绑定用户任务为 `SUCCEEDED`；未绑定用户为 `SKIPPED_UNBOUND`。

## 故障测试

- 临时停止 AiAssistant：任务应进入 `RETRYING`，恢复后只重试失败任务。
- 将 open_id 改成无效值：任务最终为 `FAILED` 并记录平台错误码。
- 重复运行调度或重启后端：唯一键保证同一工单、接收人和渠道不重复创建任务。
- 多处理人：每个未完成人员各生成一条任务，互不影响。
