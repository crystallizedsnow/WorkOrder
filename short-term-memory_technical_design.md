# 工单 Agent 短期记忆与上下文压缩技术方案

## 1. 背景

### 1.1 建设背景

当前 AiAssistant 同时存在短期会话记忆和长期记忆两套实现：短期会话记忆使用 MongoDB 按 `memoryId` 保存完整消息列表；长期记忆使用本地文件和 Markdown 索引保存从对话中提取的用户偏好与身份信息。Agent 主循环会在请求开始时加载历史，在请求结束时保存完整消息，并在长期记忆开关开启时额外加载和提取长期记忆。

前期调研确认，存量长期记忆默认关闭，且存在身份边界错误、关键词误提取、文件覆盖、合并后无法按用户加载、多实例不共享、并发不安全、缺少过期和隐私治理等问题，不具备继续启用或在原实现上扩展的条件。本期决定删除全部存量长期记忆代码，不建设新的长期记忆。

短期会话记忆仍是 Agent 连续对话、工具链执行和复杂任务续接的必要能力。但当前实现会持续累积并回传完整消息，没有实际接入上下文压缩；随着对话轮次和工具结果增加，将出现上下文超限、模型调用成本增长、旧信息干扰当前判断等问题。因此，本期需要在保留会话内连续性的前提下，建设可持久化、可过期、并发安全的短期记忆上下文压缩能力。

### 1.2 建设目标

1. 删除存量长期记忆代码、配置、测试和文档描述，消除无效能力和误导性设计。
2. 以 MongoDB 作为短期记忆的唯一持久化介质，现有会话数据直接删除，不做数据兼容和迁移。
3. 在 16K 模型上下文窗口内，动态控制系统提示词、工具定义、RAG、会话摘要、最近历史和当前请求的总上下文规模。
4. 建立“滚动摘要 + 最近完整消息”的短期记忆结构，减少历史消息占用，同时保留当前任务所需的关键信息。
5. 保证工具调用请求与工具结果的协议完整性，不因压缩产生孤立消息或错误执行语义。
6. 建立会话过期和清理机制，避免无期限保存聊天历史。
7. 复用 Channel 已落地的统一入口和飞书身份能力，并补齐 Web 用户归属、会话映射和跨入口串行，形成统一记忆隔离逻辑。
8. 支持在测试环境降低上下文窗口和触发阈值，以较少消息稳定覆盖压缩场景。

### 1.3 非目标

- 不建设跨会话用户画像、用户偏好或长期任务记忆。
- 不把短期记忆作为工单状态、权限、接口字段和校验规则的事实来源。
- 不在摘要中保存密码、Token、账号凭证或其他敏感认证信息。
- 不允许从历史摘要继承写操作授权、确认状态或已过期的 dry-run 结果。
- 不修改后端业务字段、接口语义和校验规则。
- 不在本期建设独立向量记忆库；RAG 知识库继续作为独立能力运行。

### 1.4 设计原则

- **会话内有效**：短期记忆只在同一内部会话中生效，不跨会话推断用户信息。
- **身份与会话分离**：用户身份用于鉴权和归属校验，内部会话标识用于上下文隔离，两者不得混用。
- **实时事实优先**：工单状态、权限和业务数据必须通过工具实时获取，不依赖历史摘要。
- **协议完整**：工具调用及其全部结果视为不可拆分的消息单元。
- **无损优先**：压缩失败时优先保留原始历史，不使用缺少业务信息的占位摘要覆盖历史。
- **配置驱动**：模型窗口、触发阈值、保留预算和过期时间均配置化。
- **单一职责**：Agent 主循环负责编排，记忆服务负责读取、预算、压缩和持久化。
- **安全清理**：过期数据自动清理，用户主动清除时同步删除摘要和最近消息。

### 1.5 审核后方案摘要

- **压缩方式**：模型调用前按动态 token 预算触发；先把最旧的完整对话/工具原子单元滚入结构化摘要，保留最近完整原子单元。后续只用“旧摘要 + 新进入压缩区的单元”更新摘要。摘要校验或持久化失败时不删除原文，当前请求及正在执行的工具协议不截断。
- **存储方式**：废弃 `memoryId + content(JSON 字符串)` 的整表式覆盖，改为 MongoDB 结构化会话文档，保存 owner、摘要、摘要覆盖序号、最近消息、版本、幂等请求号、状态和过期时间；写入使用归属条件与乐观锁，TTL 负责最终清理。
- **Channel 衔接**：复用已落地的 `AgentGateway`、飞书身份换票、消息幂等和确认卡片；补齐 Web 用户身份、Redis 会话映射粒度/TTL、Web 与飞书统一串行边界以及解绑/清除闭环。

## 2. 现状与问题分析

### 2.1 Channel 改造后的当前调用链（2026-08-14 代码基线）

当前代码已经不是单一 Web 入口。Web 与飞书都进入 `AgentGateway`，但两条入口在身份补全、会话映射和串行控制上仍不对称。

```text
Web 客户端
  → POST /assistant/chat（memoryId、message、Bearer Token）
  → AIChatController 直接构造 AgentRequest（userId 当前为 null）
  → AgentGateway → AgentLoop
  → MongoDB 按 memoryId 读取完整聊天历史
  → 拼接系统提示词、RAG、历史和当前用户消息
  → ChatModel 执行 ReAct 循环
  → Assistant 发起工具调用
  → ToolDispatcher 执行工具并回传 tool 消息
  → Agent 生成最终回答
  → MongoDB 覆盖保存完整消息列表

飞书私聊
  → 飞书 WebSocket → 消息解析、Redis 幂等和限流
  → ConversationExecutor 按 tenantId:botAccountId:senderId 在单实例内串行
  → 后端查询绑定关系并换取短期代理 Token
  → RedisChannelSessionStore 按 channel:tenantId:botAccountId:workorderUserId 解析 Long sessionId
  → AgentGateway → AgentLoop
  → MongoDB 按 memoryId 读取/覆盖完整聊天历史
  → 飞书文本回复，或 dry-run 后创建确认卡片并提前保存当前消息
```

长期记忆开启时，`AgentLoop` 仍会在模型调用前后分别执行本地文件加载和关键词提取。本期删除该分支，主链路只保留会话级短期记忆。

### 2.2 现状审核结论

| 领域 | 已落地现状 | 对本方案的影响 |
|---|---|---|
| 统一入口 | `AgentRequest`、`AgentGateway` 已落地，Web/飞书共用 `AgentLoop` | 记忆服务应直接接收 `AgentRequest`，不再只接收裸 `sessionId` |
| 飞书身份 | 已完成绑定查询、代理 Token 换取，`AgentRequest.userId` 可获得真实工单用户 | 飞书记忆可执行用户归属校验 |
| Web 身份 | Controller 只校验 Bearer 形式，构造请求时 `userId=null` | 在接入新存储前必须补齐 Token 对应用户解析；不能以 null 绕过归属校验 |
| 会话映射 | Redis 键实际为 Channel、租户、机器人和工单用户；没有源会话 ID，也没有 TTL | 当前语义是“同一用户在同一机器人下共享一个会话”，不是“每个飞书会话一个记忆”；需先明确并修正键模型 |
| 串行执行 | `ConversationExecutor` 仅在当前 JVM 内按飞书发送者串行 | 可降低单实例竞争，但不是持久化队列/分布式锁；MongoDB 仍必须乐观并发控制，Web 也需进入同一串行边界 |
| 消息幂等 | 飞书消息使用 Redis claim | 防止同一事件重复进入 Agent，但不能替代记忆写入幂等和版本控制 |
| 写确认 | dry-run 后会在中途保存完整消息并返回空回答；卡片回调绕过 AgentLoop 直接执行 CLI | “一轮只在最终回答后保存”不成立；必须支持检查点保存，并把确认结果作为安全审计事件处理 |
| 短期存储 | MongoDB `chatMessages` 仍是 `memoryId + content(JSON 字符串)`，整体读写、无版本、无 owner、无 TTL | 需要替换存储模型，不能仅在现有 `content` 内追加摘要字段 |
| 压缩 | `ContextCompressor`/`CompressingChatMemory` 未接入主链路；按消息数、字符数/4、固定保留 10 条处理 | 应删除包装器并重写压缩服务，不能把现有压缩算法视为可复用实现 |
| 长期记忆 | 代码、配置和主链路仍存在，默认关闭不等于已删除 | 仍需按本方案执行存量清理 |

### 2.3 短期记忆现状

| 项目 | 当前实现 | 问题 |
|---|---|---|
| 存储模型 | `chatMessages` 中一个 `memoryId` 对应一份 `content` JSON 字符串 | 消息持续增长，每次都整体读写；无法对消息或摘要原子更新 |
| 上下文加载 | 每轮加载全部历史 | 长对话会超出模型窗口 |
| 压缩组件 | 存在独立压缩类和内存包装类 | 未接入 Agent 主调用链，也未持久化摘要 |
| 触发判断 | 固定消息数和粗略字符估算 | 中文 token 被低估，工具参数未计入 |
| 工具消息 | 与普通消息统一按条处理 | 简单截断可能拆散 tool call 与 tool result |
| 并发更新 | 读取后整体覆盖 | 同一会话并发请求可能丢失消息 |
| 生命周期 | 没有自动过期 | 历史长期保留，存储和隐私风险持续增加 |
| 错误处理 | 存储异常主要记录日志 | 调用方可能误以为记忆已经保存 |

### 2.4 长期记忆删除原因

存量长期记忆把请求中的会话标识当作用户标识，不能实现可信的跨会话用户记忆；提取逻辑依赖固定关键词，无法区分用户偏好、临时需求、助手回复和工具结果；本地文件方案不能适应并发和多实例部署；数据没有过期、删除、审计和敏感信息控制能力。因此，本期不对该实现进行修补，直接删除对应代码和配置。

### 2.5 与 Channel 现状的关系

Channel 改造已经落地统一 Agent 入口、飞书身份换票、Redis 消息幂等、Redis 会话映射和单实例串行执行，但原设计中以下能力尚未完整落地：

- Web 请求没有解析并填充真实 `workorderUserId`；
- Redis 会话映射没有保存 owner 元数据和 TTL，也没有使用 `sourceConversationId`；
- `ConversationExecutor` 是 JVM 内队列，Web 请求没有经过该队列，多实例也不共享队列；
- 解绑、换绑、主动清除和会话过期没有与聊天记忆清理形成闭环；
- MongoDB 聊天记录没有 owner、版本、过期时间和压缩元数据。

本方案复用已经落地的 `AgentRequest`、`AgentGateway`、飞书绑定换票和 Redis 幂等，不重复建设这些能力。记忆模块必须补齐并统一身份、会话和并发契约：所有 Channel 在进入记忆服务前都提供非空 `workorderUserId`；会话映射明确采用“平台会话”还是“平台用户”粒度；Web 和飞书使用相同的会话串行键；MongoDB 以 `sessionId + workorderUserId` 校验归属并用版本控制防止覆盖。

## 3. 改造后完整链路

### 3.1 请求进入链路

```text
Web 或飞书 Channel
  → 鉴权层解析真实 workorderUserId，Channel 层构造统一 AgentRequest
  → 校验 workorderUserId 与内部 sessionId 的归属
  → 进入统一 sessionId 串行执行边界（Web 与飞书一致）
  → SessionMemoryService 读取 MongoDB 会话状态
  → 校验并修复可恢复的消息链完整性
  → 计算本次模型输入的动态 token 预算
  → 达到阈值时执行滚动摘要并持久化压缩结果
  → 组装系统提示词、历史摘要、RAG、最近完整历史和当前消息
  → AgentLoop 执行 ReAct 循环
```

### 3.2 工具执行链路

```text
Assistant 生成 tool calls
  → 保存为当前运行内存中的 assistant 工具调用消息
  → ToolDispatcher 逐项执行
  → 每个执行结果以对应 toolCallId 回传
  → 同一批 tool calls 与全部 tool results 组成不可拆分单元
  → Agent 基于完整结果继续推理
  → 最终回答完成当前轮次
```

当前请求执行期间不对正在形成的工具调用单元做摘要，避免破坏模型工具协议。若单次工具结果本身过大，则由工具返回分页、结果引用或统一的大结果保护信息，不允许压缩器静默删除当前工具结果中的关键字段。

### 3.3 请求结束链路

```text
Agent 生成最终回答或明确失败结果
  → SessionMemoryService 将本轮完整消息追加到会话
  → 带版本条件写入 MongoDB
  → 更新最后活跃时间和过期时间
  → 返回 Channel
```

摘要默认在下一次请求开始时按需生成，不增加当前最终回答的尾部延迟。飞书 dry-run 转确认卡片属于例外检查点：在提前结束 AgentLoop 前持久化当时已经形成的完整工具单元及 `WAITING_CONFIRMATION` 状态。卡片确认/取消结果作为独立审计事件追加，不把历史确认文本转化为下一次 Agent 的执行授权。保存失败必须进入可观测错误处理，不能仅记录日志后按成功处理。

### 3.4 会话过期链路

```text
会话最后一次成功读写
  → 刷新最后活跃时间和过期时间
  → MongoDB TTL 到期
  → 自动删除会话摘要和最近消息
  → 下一次请求按新会话处理
```

用户主动清除上下文时，立即删除该会话的摘要、消息和压缩元数据；飞书解绑或换绑按飞书方案创建新会话，旧会话按过期策略清理。

## 4. 功能设计

### 4.1 长期记忆删除

删除范围包括：

1. 本地文件长期记忆服务及其测试。
2. AgentLoop 中长期记忆的配置读取、服务依赖、目录初始化、加载、系统提示词注入和对话提取逻辑。
3. 应用配置中的长期记忆开关和本地目录配置。
4. `.memory`、长期记忆 JSON 和合并文件相关忽略规则。
5. README 和 AiAssistant 技术设计中长期记忆的架构、功能、测试和阶段计划描述。
6. 仅为旧长期记忆生成的本地文件不由应用升级过程自动删除；部署环境确认后执行一次性人工清理。

删除后，代码和文档中不再把短期会话历史称为用户长期记忆，也不保留关闭状态的备用实现。

### 4.2 会话记忆模型

每个内部 sessionId 对应一份会话记忆。首期继续保留现有 `Long sessionId`，避免同时改动 Todo、Hook、确认单和工具自动注入接口；但存储 API 改为显式接收 `sessionId` 与 `workorderUserId`，不再使用无类型的 `Object memoryId`。MongoDB 文档至少包含以下逻辑字段：

| 字段 | 用途 |
|---|---|
| sessionId | 内部会话唯一标识 |
| workorderUserId | 会话归属用户，用于隔离和校验 |
| summary | 已压缩历史的结构化滚动摘要 |
| summaryVersion | 摘要格式版本，支持后续演进 |
| summaryThroughSequence | 摘要已经覆盖到的消息序号 |
| recentMessages | 尚未压缩的最近完整消息 |
| version | 乐观锁版本，避免并发覆盖 |
| createdAt | 会话创建时间 |
| updatedAt | 最后更新时间 |
| expiresAt | 自动过期时间 |
| lastCompressedAt | 最近一次成功压缩时间 |
| state | `ACTIVE`、`WAITING_CONFIRMATION` 或 `INTERRUPTED` |
| lastRequestId | 最近成功追加的请求/事件幂等标识 |

消息应具有稳定递增序号，并保留角色、正文、工具调用集合、toolCallId 和工具名称等协议字段。动态系统提示词和当前请求生成的 RAG 内容不保存到会话历史，避免重复注入和把临时知识固化为历史事实。

由于已经确认不保留当前 MongoDB 会话记录，本期发布前直接删除旧 `chatMessages` 集合数据，再以结构化 BSON 字段重建会话集合；不实现旧 `content` JSON 字符串的兼容读取。推荐索引为：`sessionId` 唯一索引、`{workorderUserId:1, updatedAt:-1}` 查询索引、`expiresAt` TTL 索引。写入条件必须同时包含 `sessionId`、`workorderUserId` 和 `version`。

### 4.3 滚动摘要

短期记忆采用“结构化滚动摘要 + 最近完整原子单元”的两层压缩：

- **滚动摘要**：保存较早历史中仍影响当前任务的信息；
- **最近完整消息**：保存最近若干完整对话轮次和工具调用链。

首次压缩时，从最旧端选择若干完整原子单元生成摘要；后续压缩时，仅使用“旧摘要 + 本次新进入压缩区的完整原子单元”滚动生成新摘要。新摘要成功通过结构、敏感信息和覆盖序号校验后，才以同一版本条件原子替换摘要并删除已经覆盖的 `recentMessages`；失败时旧摘要和原消息均不变。

#### 4.3.1 压缩触发与裁剪顺序

每次模型调用前，先估算固定开销、系统提示词、工具定义、RAG、摘要、最近历史、当前用户消息和输出预留。预计总量超过触发阈值时，按以下顺序处理：

1. 按完整原子单元从历史最旧端滚入摘要，直到最近历史回到目标预算。
2. 若滚动摘要本身超过摘要上限，用“旧摘要 + 新增单元”重写为受限结构，不对摘要反复套娃拼接。
3. 若仍超预算，从最旧端继续移除已经进入摘要的完整单元，并按独立上限缩减 RAG。
4. 当前用户消息和当前正在执行的工具协议不截断；单项本身超限时返回明确错误或结果引用。

最终送入模型的顺序固定为：动态系统提示词 → 历史摘要（标注非实时事实）→ 最近完整原子单元 → 当前请求/当前工具单元。摘要不是伪造的 `assistant` 历史回答，也不复用现有压缩器生成的普通 system 文本覆盖真实系统提示词。

摘要至少覆盖以下信息类别：

- 当前目标和用户明确约束；
- 用户已经确认的业务选择，但不包含可复用的执行授权；
- 已完成事项及必要业务标识；
- 关键工具结果、数据时间和结果来源；
- 未完成事项、失败原因和待补充信息；
- 当前任务继续执行所需的文件、工单或流程标识。

摘要不得包含：

- Token、密码、密钥和完整认证信息；
- 模型推测出的用户身份或偏好；
- 已经过期的实时工单状态；
- 可以被解释为新一次写操作授权的历史确认；
- 与当前会话任务无关的大段工具原文。

摘要需要明确区分“历史记录”和“当前事实”。Agent 使用摘要中的工单状态、权限或业务数据前，仍需调用工具重新查询。

### 4.4 消息原子单元

压缩前先由会话分段组件识别不可拆分单元：

| 单元类型 | 组成 |
|---|---|
| 普通对话轮次 | 用户消息及对应最终助手回答 |
| 工具轮次 | 用户消息、assistant tool calls、全部对应 tool results、后续助手回答 |
| 多工具轮次 | 一批 tool calls 与所有匹配 toolCallId 的结果 |
| 系统门禁轮次 | 触发门禁的当前请求、门禁消息和后续处理结果 |
| 中断轮次 | 已形成的工具链、已取得结果和明确中断状态 |

压缩边界只能位于原子单元之间。任何进入最近历史的 tool 消息都必须能找到对应的 assistant tool call；任何保留的 tool call 都必须保留其已产生的全部 tool result。发现无法恢复的不完整历史时，记录诊断信息并将该单元转为明确的中断摘要，不直接把非法消息结构发送给模型。

### 4.5 上下文预算

生产默认模型上下文窗口按 16K token 设计。每次请求动态计算可用于历史的预算，预算组成包括：

| 上下文部分 | 处理策略 |
|---|---|
| 模型输出预留 | 从总窗口中固定预留，保证模型能够完成回答 |
| 系统提示词 | 每次按实际内容估算 |
| 工具定义 | 包含所有工具名称、描述和参数 Schema |
| RAG 上下文 | 按实际检索结果估算，并设置独立上限 |
| 当前用户消息 | 必须完整保留，超限时直接提示拆分请求 |
| 历史摘要 | 设置摘要最大预算 |
| 最近完整历史 | 使用剩余预算，按完整原子单元从新到旧保留 |
| 安全余量 | 应对 tokenizer 估算误差和协议封装开销 |

建议初始预算按以下比例实施，最终值通过测试校准：

- 模型最大上下文：16K；
- 最大输出预留：约 4K；
- 输入安全余量：不少于 1K；
- 压缩触发点：预计总上下文达到约 12K；
- 最近完整历史目标：约 4K 至 5K；
- 滚动摘要上限：约 2K；
- RAG 和工具定义使用剩余输入预算，并分别设置保护上限。

阈值全部配置化，不与具体模型名称或固定消息条数绑定。启动时校验各预算关系，避免最大输出、触发阈值和上下文窗口配置互相冲突。

测试环境允许将窗口调整到较小值，并按比例降低触发阈值、最近历史预算、摘要上限和输出预留，以少量对话稳定触发压缩。测试不得依赖发送超长无意义文本才能进入压缩分支。

### 4.6 Token 估算

统一 token 估算组件必须覆盖：

- 中文、英文、数字和标点正文；
- system、user、assistant 和 tool 消息的协议开销；
- assistant tool calls 的名称和参数；
- toolCallId、工具名称和工具返回内容；
- 工具定义及参数 Schema；
- RAG 内容；
- 模型请求的固定封装开销。

首期采用偏保守的通用估算策略，并增加安全余量；不能继续使用“总字符数除以固定数值”的单一算法。若后续模型客户端提供可靠 tokenizer，只替换估算实现，不改变预算、分段和压缩流程。

### 4.7 工具大结果保护

工具结果是上下文增长的主要来源。处理分为当前轮和历史轮两个阶段：

1. 当前轮工具结果优先保持完整，让 Agent 能够完成本次推理。
2. 数据查询优先通过分页、筛选和文件输出减少单次返回规模。
3. 单个工具结果超过当前模型安全预算时，由统一保护逻辑保留执行状态、错误码、总数、分页信息、关键标识和结果引用，并明确标记内容经过裁剪。
4. 工具结果进入旧历史后，可随完整工具轮次进入滚动摘要。
5. 写操作摘要只保留动作、目标、结果和业务标识，不保留可用于再次执行的授权语义。

工具具体字段和分页语义仍以 CLI Service 运行时 Schema 及后端实现为准，不在记忆模块维护接口级规则。

### 4.8 会话过期与清理

短期记忆必须自动过期。建议默认采用最后活跃时间后 30 天过期，并允许按环境配置调整。每次成功读取或写入会话时刷新 expiresAt；MongoDB 使用 TTL 索引自动删除到期文档。

同时提供统一的会话清除能力，供以下场景调用：

- 用户主动开始新会话或清除上下文；
- 飞书账号解绑或换绑；
- 管理员按合规要求清理指定会话；
- 测试数据清理。

清理操作必须校验当前用户对会话的归属，删除摘要、完整消息及压缩元数据。TTL 删除允许存在 MongoDB 后台任务粒度造成的短暂延迟，业务读取时仍需先判断 expiresAt，已过期会话不得继续返回历史。

### 4.9 并发与一致性

现有 `ConversationExecutor` 只保证单实例内同一飞书发送者串行，且 Web 不经过该执行器。因此改造时应将串行入口下沉到 `AgentGateway` 或统一会话执行服务，以 `sessionId` 为键覆盖 Web、飞书和确认相关的记忆写入。若部署保持单实例，可先复用当前执行器实现；多实例上线前必须改为 Redis 分布式锁/队列或等价机制。

保存时按 sessionId、workorderUserId 和当前版本进行条件更新；发生版本冲突时重新读取最新状态，合并本轮尚未保存的完整消息后有限重试。压缩和消息追加共用同一版本机制，不允许摘要覆盖并发新增消息。

不同 sessionId 之间允许并行。无论是否已有串行执行，MongoDB 仍使用乐观锁作为最终数据一致性保障；Redis 消息幂等只处理飞书事件重复投递，存储层还要用 `lastRequestId` 防止重试重复追加。

### 4.10 异常与降级

| 异常 | 处理方式 |
|---|---|
| 摘要模型调用失败 | 保留原始历史，本次优先按完整单元缩减发送范围，不覆盖已有摘要 |
| 摘要结构不合法 | 拒绝持久化新摘要，继续使用旧摘要和原始历史 |
| MongoDB 读取失败 | 本次按无历史模式执行并返回可观测告警；涉及依赖历史确认的操作必须停止 |
| MongoDB 保存失败 | 明确记录保存失败并上报指标，不得伪装记忆持久化成功 |
| 版本冲突 | 重新读取、合并本轮增量并有限重试 |
| 历史工具链不完整 | 转为中断摘要并隔离非法消息，不直接发送给模型 |
| 当前消息单独超限 | 不截断用户语义，提示用户拆分请求或改用文件方式 |
| 摘要后仍超预算 | 继续减少最近完整单元；必要时缩减 RAG，不删除当前消息和工具协议必要内容 |

降级过程不得把历史 dry-run、确认消息或摘要解释为当前写操作授权。写操作仍执行现有发现、Schema 查询、参数构建、预演、确认和执行流程。

### 4.11 可观测性

每次上下文准备记录以下非敏感指标：

- sessionId、Channel 和 traceId；
- 压缩前后消息单元数；
- 各上下文部分的估算 token；
- 是否触发压缩、触发原因和耗时；
- 摘要版本及覆盖序号；
- 最近历史保留范围；
- MongoDB 版本冲突和重试次数；
- 摘要失败、结构校验失败和降级次数；
- 会话主动清理和过期清理数量。

日志不得输出完整 Token、密码、工具敏感结果或完整会话摘要。监控应能区分模型业务调用与摘要调用的耗时和 token 成本。

## 5. 实施方案

### 5.1 AiAssistant 改造

1. 删除长期记忆服务、关联测试、AgentLoop 调用和应用配置。
2. 删除未接入主链路的内存型压缩包装器，避免出现两套短期记忆状态。
3. 重构现有上下文压缩器，使其只承担滚动摘要生成和摘要结构校验。
4. 将 `ChatMemoryStore<Object>` 替换为显式的会话仓储接口，新增统一会话记忆服务，负责读取、归属校验、预算、分段、压缩、检查点保存、追加和清理。
5. 新增统一 token 估算组件，覆盖消息、工具定义、工具参数和 RAG。
6. 新增会话分段组件，建立工具调用原子单元。
7. `AgentLoop` 改为通过会话记忆服务准备上下文和保存本轮增量，不再直接调用 `getMessages/updateMessages`；飞书确认卡片分支改用检查点 API。
8. AgentLoop 只把稳定会话历史交给记忆服务；动态系统提示词、RAG 和运行期提醒不作为普通历史重复持久化。
9. Web 入口通过可信 Token 校验结果填充 `AgentRequest.userId`，禁止以 null owner 读取或创建持久化记忆。
10. 将会话串行控制收敛到统一 Agent Gateway 边界，使 Web 和飞书使用相同 `sessionId` 键；保留 MongoDB 乐观锁。
11. 将存储异常从静默日志调整为明确、可观测的执行状态。

### 5.2 MongoDB 改造

1. 发布前删除现有聊天会话数据，不执行旧结构迁移。
2. 按新会话记忆模型重建集合结构，使用结构化 BSON 保存 `summary`、`recentMessages` 和元数据，不再把整个列表序列化到 `content` 字符串。
3. 为内部 sessionId 建立唯一索引。
4. 为 expiresAt 建立 TTL 索引。
5. 增加 workorderUserId 归属、版本、摘要覆盖序号和时间字段。
6. 使用 `{sessionId, workorderUserId, version}` 条件更新实现乐观锁，使用 `$inc` 推进版本；禁止无版本整体覆盖。
7. 为开发、测试和生产分别配置过期时间，测试环境使用更短周期验证过期行为。

### 5.3 Channel 衔接改造

直接复用已经落地的统一 `AgentRequest/AgentGateway`、飞书绑定换票、消息幂等、确认卡片和异步回复。以下项目属于短期记忆上线的前置补齐项：

1. Web 从有效 Bearer Token 获取 `workorderUserId` 并填入 `AgentRequest`。
2. 修正 Redis 会话键。默认按 `channel:tenantId:botAccountId:sourceConversationId:workorderUserId` 隔离；若产品明确选择“每用户单一连续会话”，才可省略 `sourceConversationId`，并必须在文档和 UI 中公开该语义。
3. 为 Redis 会话映射设置与短期记忆一致或更长的 TTL；解绑/换绑时删除旧映射或写入新的会话代次，保证不会重新解析到旧 `sessionId`。
4. 将串行执行键改为内部 `sessionId`，并覆盖 Web 与飞书；当前按 `senderId` 的 JVM 队列只能作为单实例阶段实现。
5. 增加用户主动清除入口，调用带 owner 校验的记忆删除和会话映射轮换。
6. 飞书卡片回调保持独立授权流程；只向记忆追加确认结果，不通过历史消息再次触发命令。

短期记忆模块只依赖内部 `sessionId`、`workorderUserId`、`channel` 和 `requestId/traceId`，不依赖飞书 SDK 类型。

### 5.4 配置方案

配置应至少覆盖：

- 短期记忆开关；
- 模型最大上下文窗口，生产默认 16K；
- 最大输出预留；
- 压缩触发阈值；
- 最近完整历史目标预算；
- 摘要最大预算；
- RAG 最大预算；
- 单个工具结果保护阈值；
- token 估算安全余量；
- 会话过期时间，默认建议 30 天；
- 乐观锁最大重试次数；
- 摘要调用超时。

测试配置使用较小上下文窗口和按比例缩小的各项预算，以确定性触发压缩、二次滚动压缩和极限降级路径。

### 5.5 文档同步

同步更新：

- README 中的记忆能力和依赖说明；
- AiAssistant 总体技术设计中的记忆模块、Agent 调用链、测试项和实施阶段；
- 飞书 Channel 技术方案中“长期记忆和短期聊天历史”的表述，删除长期记忆依赖，只保留短期会话记忆；
- 部署说明中的 MongoDB 索引初始化、旧数据删除、TTL 和配置项；
- 测试说明中的压缩触发、过期和清理步骤。

## 6. 测试与验证

### 6.1 单元测试

1. 中文、英文、混合文本、工具参数和工具 Schema 的 token 估算。
2. 未达到阈值时不压缩。
3. 达到 token、消息单元或单结果保护阈值时正确触发。
4. 普通轮次、单工具、多工具和中断工具链的原子分段。
5. tool call 与 tool result 在压缩后保持完整对应。
6. 首次摘要和二次滚动摘要只覆盖目标消息范围。
7. 摘要保留任务目标、约束、业务标识和待办事项。
8. 摘要移除凭证和可复用写操作授权。
9. 摘要失败或结构错误时不覆盖旧数据。
10. 动态系统提示词和 RAG 不进入持久化历史。

### 6.2 存储测试

1. 新会话创建、读取、追加、压缩和删除。
2. sessionId 唯一索引和 workorderUserId 归属校验。
3. 两个并发更新发生版本冲突时不丢消息。
4. 压缩与追加并发时摘要覆盖范围正确。
5. expiresAt 刷新和 TTL 自动删除。
6. 业务读取拒绝返回已经逻辑过期但尚未被 TTL 后台任务物理删除的数据。
7. 主动清理同时删除摘要、消息和压缩元数据。
8. MongoDB 不可用、写入失败和重试耗尽场景。
9. 同一 `requestId` 重放时不重复追加消息。
10. Web 请求缺少可信 `workorderUserId` 时拒绝持久化，不产生无 owner 会话。

### 6.3 Agent 集成测试

1. 在小窗口测试配置下，用少量多轮对话触发首次压缩。
2. 连续对话触发二次滚动压缩，旧摘要不会重复膨胀。
3. 压缩后 Agent 仍能识别当前目标、用户约束、工单号和未完成步骤。
4. 压缩后涉及实时状态时仍调用工具查询，不直接使用摘要作答。
5. 包含多次工具调用的长任务压缩后协议合法。
6. 历史 dry-run 和确认消息压缩后不能授权新的写操作。
7. 摘要调用失败时当前请求仍能安全降级。
8. 删除长期记忆后应用启动、普通对话和工具调用正常。

### 6.4 Channel 与隔离测试

1. Web 和飞书入口进入同一套会话记忆服务。
2. 不同用户不能通过伪造 sessionId 读取或删除他人历史。
3. 同一会话连续消息按顺序执行，不发生上下文覆盖。
4. 不同会话并行执行互不影响。
5. 飞书解绑或换绑后不继承旧用户会话。
6. 用户主动清除后下一条消息按新上下文处理。
7. 飞书不同 `sourceConversationId` 按选定会话粒度正确隔离；Redis 映射过期后不会串接其他用户或旧绑定会话。
8. 飞书 dry-run 检查点、确认、取消和执行失败均能追加正确状态，历史确认不能授权新的写操作。
9. Web 与飞书对同一内部 sessionId 的并发请求进入同一串行边界。

### 6.5 过期测试

测试环境将过期时间配置为分钟级或更短的可控值，验证：

1. 活跃会话刷新过期时间；
2. 到期后业务立即视为无历史；
3. MongoDB TTL 最终完成物理删除；
4. 已过期摘要不会再次注入模型；
5. 到期后使用相同外部平台会话时，根据 Channel 会话策略创建或初始化新的短期上下文。

### 6.6 验收标准

- 仓库中不存在存量长期记忆运行代码、配置和测试。
- 文档不再声明系统具备用户长期记忆能力。
- 生产配置以 16K 上下文窗口进行预算控制。
- 测试环境可通过小窗口配置稳定触发全部压缩分支。
- 长会话多次压缩后仍保留当前任务关键语义。
- 任何压缩结果都不会破坏工具调用协议。
- 历史摘要不能绕过实时查询、权限校验、dry-run 和用户确认。
- 同一会话并发请求不丢消息，不同用户不能串读会话。
- 会话达到配置的非活跃期限后自动失效并最终物理删除。
- MongoDB 或摘要模型异常时有明确降级、日志和监控，不静默丢失会话状态。

## 7. 实施阶段

### 阶段一：存量清理

- [x] 删除长期记忆代码、配置和测试引用。
- [x] 删除旧短期压缩包装器和失效设计描述。
- [ ] 在发布维护窗口备份（如需要）并删除 MongoDB `chatMessages` 旧会话数据；该操作不得在开发改造时自动执行。
- [x] 完成删除后的编译和基础回归测试。

阶段一代码清理于 2026-08-14 完成。MongoDB 数据清理保留为发布操作，因为现有集合删除不可恢复，且新结构将在阶段二建立；发布前应由部署负责人确认目标数据库和备份策略后执行。

### 阶段二：会话模型与存储

- [x] 建立新的 MongoDB `sessionMemories` 结构化会话记忆模型。
- [x] 建立 sessionId 唯一索引、expiresAt TTL 索引和 version 乐观锁。
- [x] 实现会话读取、增量追加、删除、逻辑过期和非空用户归属校验。

阶段二代码于 2026-08-14 完成。旧 `chatMessages` 集合不再被运行时代码引用；物理删除仍按阶段一约定在发布维护窗口执行。Web 请求当前尚未提供 `workorderUserId`，因此 Web 强制归属补全属于阶段四前置项，飞书请求已执行非空归属校验。

### 阶段三：预算与压缩

- [x] 实现统一保守 token 估算和动态上下文预算。
- [x] 实现消息原子分段和工具链完整性校验。
- [x] 实现结构化滚动摘要、敏感信息校验和失败保留原文降级。
- [x] 实现工具大结果保护。

阶段三代码于 2026-08-14 完成。摘要仅在达到预算阈值时调用模型，成功校验并以乐观锁写入后才删除覆盖范围内的原始消息；不完整工具单元不会注入模型。单个超预算工具结果保留状态、分页及关键业务标识，并明确标记为已裁剪。

### 阶段四：Agent 与 Channel 接入

- [x] AgentLoop 接入统一会话记忆服务。
- [x] Web 通过后端 `/api/auth/validate` 补齐可信用户身份，Redis 会话映射键加入源会话并刷新 TTL。
- [x] Web、飞书和确认检查点在 Agent Gateway 按统一 sessionId 串行执行。
- [x] 提供带用户归属校验的 Web 会话主动清除接口；飞书映射过期后生成随机新 sessionId，解绑/换绑不继承其他用户会话。

阶段四代码于 2026-08-14 完成。当前串行协调器适用于单 AiAssistant 实例；多实例部署前仍需替换为 Redis 分布式锁/队列，但 MongoDB 乐观锁继续作为最终一致性保护。

### 阶段五：验证与发布

- [x] 使用小窗口配置完成压缩、滚动压缩、工具原子性、超大结果和失败保留原文测试。
- [ ] 发布前使用 16K 生产参数完成长对话和工具链回归。
- [x] 完成 TTL、同 sessionId 串行、用户隔离、幂等和主动清理的自动化测试。
- [x] 更新短期记忆方案、总体技术设计和相关能力说明。
- [x] 完成真实模型与写服务场景一回归：dry-run、结构化确认、真实创建、无无关后续说明均通过；创建工单 `WO202608142088083893189218304`（ID `74`）。

#### 本地压缩测试阈值

当前默认值已调整为便于手工测试的“小窗口档”，并全部支持通过环境变量覆盖：

| 配置 | 默认值 | 环境变量 | 说明 |
|---|---:|---|---|
| context-window | 2048 | MEMORY_CONTEXT_WINDOW | 固定短期记忆逻辑窗口，不随测试用例调整 |
| compression-trigger | 1600 | MEMORY_COMPRESSION_TRIGGER | 达到固定窗口的压缩触发线 |
| recent-history-target | 300 | MEMORY_RECENT_HISTORY_TARGET | 压缩后最近完整历史目标 |
| summary-max-tokens | 200 | MEMORY_SUMMARY_MAX_TOKENS | 滚动摘要上限 |
| max-output-tokens | 1024 | MEMORY_MAX_OUTPUT_TOKENS | 输出预留；LLM_MAX_TOKENS 默认同步为 1024 |
| safety-margin | 128 | MEMORY_SAFETY_MARGIN | token 估算安全余量 |
| max-tool-result-tokens | 3000 | MEMORY_MAX_TOOL_RESULT_TOKENS | 单工具结果保护阈值；不随测试窗口缩到 400，避免裁剪命令列表和 Schema |

生产回归建议覆盖为：16384、12288、4600、2000、4096、1024、3000，并确保模型自身的最大输出配置与 `max-output-tokens` 一致。

## 8. 风险与控制

| 风险 | 影响 | 控制措施 |
|---|---|---|
| 摘要遗漏关键业务标识 | 后续任务无法续接 | 结构化摘要、关键标识规则、最近完整消息和回归测试 |
| 摘要产生错误事实 | Agent 使用错误历史 | 标记历史数据时间，实时业务必须重新查询，摘要结构校验 |
| 工具链被拆分 | 模型协议错误或重复执行 | 原子分段，只允许在完整轮次之间压缩 |
| 估算 token 偏差 | 请求仍可能超限 | 保守估算、安全余量、模型超限后的降级和监控 |
| 摘要调用增加成本 | 延迟和模型费用增加 | 仅达到阈值时调用，滚动压缩，独立统计摘要调用 |
| 并发覆盖会话 | 消息丢失或顺序错误 | 统一 Web/飞书串行边界并增加 MongoDB 乐观锁 |
| TTL 删除存在延迟 | 已到期数据短时仍在库中 | 业务读取先判断 expiresAt，TTL 负责最终物理清理 |
| 旧数据直接删除 | 原对话不可恢复 | 发布前明确备份或清理窗口，本方案不提供兼容迁移 |
| Web `userId` 仍为空 | 可伪造 memoryId 读取他人历史 | 记忆持久化前强制从可信 Token 解析用户并校验 owner |
| Redis 映射键不含源会话且无 TTL | 多个飞书会话共享历史，旧绑定长期复用 | 明确会话粒度，修正键并设置 TTL/绑定代次 |
| 串行队列仅在单 JVM 且 Web 未接入 | 多实例或跨入口并发覆盖 | 统一 Gateway 串行边界 + MongoDB 乐观锁；扩容前使用分布式实现 |

## 9. 最终决策

1. 删除存量长期记忆，不保留关闭代码或旧测试。
2. 当前 MongoDB 会话记录不保留，发布时直接删除并使用新结构。
3. 短期记忆必须支持自动过期，默认建议最后活跃 30 天后失效，具体期限保持配置化。
4. 复用 Channel 已落地的统一入口、飞书身份和消息幂等；Web 身份补全、会话映射 TTL/粒度、跨入口串行和解绑清理必须作为本方案前置改造完成。
5. 模型上下文窗口按 16K 设计，所有预算配置化。
6. 测试环境使用小窗口和小阈值稳定验证压缩，不依赖超长测试文本。
7. 不建设向量长期记忆。
8. 工单业务事实、权限和接口契约仍以实时后端与 CLI Service Schema 为准。
9. 压缩结果采用结构化滚动摘要与最近完整原子单元组合；只在新摘要校验和带版本写入成功后删除被覆盖原文。
10. MongoDB 改为结构化会话文档，以 `sessionId` 唯一定位、`workorderUserId` 校验归属、`version` 乐观锁、`expiresAt` TTL 和 `lastRequestId` 幂等控制替代 `memoryId + content` 整体覆盖。
